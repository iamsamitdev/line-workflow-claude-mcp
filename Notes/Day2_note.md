# LINE Workflow Automation with Claude Code & MCP - วันที่ 2: Auto Workflow เต็มระบบ

**หลักสูตรอบรมออนไลน์เชิงปฏิบัติการ: สร้างระบบแจ้งเตือน สรุปแชทกลุ่ม และรายงานอัตโนมัติบน LINE ด้วย AI Agent สำหรับองค์กรไทย**
**วันที่ 2: เก็บแชทกลุ่ม สรุปงาน และรายงานอัตโนมัติ**
วันอาทิตย์ที่ 9 สิงหาคม 2569 | เวลา 20:30 - 23:30 น. | อบรมออนไลน์สดผ่าน Zoom
ผู้สอน: อ.สามิตร โกยม | จัดอบรมโดย สถาบันไอทีจีเนียส เอ็นจิเนียริ่ง

---

## 🎯 วัตถุประสงค์การเรียนรู้ประจำวัน

เมื่อจบการอบรมวันที่ 2 ผู้เรียนจะสามารถ:

1. อธิบายสถาปัตยกรรม **4 ส่วน** ของระบบ Auto Workflow ได้ (Webhook ขาเข้า + PostgreSQL หน่วยความจำ + Postgres MCP ขาอ่าน + LINE Bot MCP ขาส่งกลับ)
2. ออกแบบ **schema ฐานข้อมูล** สำหรับเก็บบทสนทนากลุ่ม และอธิบายเหตุผลของทุกคอลัมน์สำคัญได้
3. อ่านและอธิบายการทำงานของ **Webhook Server** (Node.js + Express + TypeScript + @line/bot-sdk) ในชุดไฟล์ที่ clone มา ที่รับข้อความจากไลน์กลุ่มและบันทึกลง PostgreSQL ได้จริง
4. เปิดระบบในเครื่องสู่อินเทอร์เน็ตด้วย **ngrok** และตั้งค่า Webhook URL ใน LINE Developers Console ได้
5. **ตรวจโค้ดตามหัวข้อความปลอดภัยและความทนทาน 6 ข้อ** และชี้ได้ว่าโค้ดจัดการแต่ละข้ออย่างไร ไม่ใช่แค่รันแล้วผ่าน
6. เชื่อม **Postgres MCP** เข้า Claude Code แล้วสั่งวิเคราะห์บทสนทนาด้วยภาษาไทยได้ (สรุปประเด็น หาผู้พูดมากสุด สกัดงานที่มอบหมาย หาประเด็นค้าง)
7. ส่ง **สรุปประชุมกลับเข้ากลุ่ม** เป็น Flex Message ที่สมาชิกทุกคนเห็นพร้อมกัน
8. ใช้ **Admin Dashboard** ทำงานเดิมโดยไม่ต้องพิมพ์ prompt เพื่อส่งมอบระบบให้ผู้ใช้ที่ไม่ใช่ Developer
9. อ่านและใช้งานสคริปต์ **daily-report** ที่ query ยอดขาย ตรวจรายการผิดปกติ (Anomaly Detection) และส่ง Flex เข้ากลุ่มผู้บริหาร พร้อมทดสอบด้วย `--dry-run`
10. ตั้งเวลารันอัตโนมัติทุกเช้าด้วย **Windows Task Scheduler** และเลือกตัวตั้งเวลาที่เหมาะกับงานได้จาก 5 ทางเลือก (Task Scheduler, cron, in-process scheduler, GitHub Actions, Claude Cloud Routines)
11. แยกแยะได้ว่า **งานแบบไหนควรสั่ง AI สด งานแบบไหนควรเป็นสคริปต์ตั้งเวลา**
12. อธิบายแนวปฏิบัติด้าน **Privacy และ Governance** ของการนำข้อมูลแชทและข้อมูลธุรกิจมาประมวลผลด้วย AI

> **หมายเหตุสำคัญของวันนี้:** วันแรกเราสั่ง AI **ส่ง** ข้อความได้แล้ว วันนี้เราจะเติมส่วนที่ขาดไปคือ **"ขารับ"** และ **"หน่วยความจำ"** ซึ่งจะทำให้ระบบครบวงจรและใช้งานจริงในองค์กรได้ ข้อมูลทุกอย่างเป็นข้อมูลจำลองของบริษัท "สยามสมาร์ทเทรด" ไม่เกี่ยวข้องกับบุคคลหรือบริษัทจริงใด ๆ

---

## 🧭 กำหนดการวันที่ 2 (โดยสังเขป)

| เวลา | นาที | หัวข้อ |
| ----------- | --- | ------------------------------------------------------------- |
| 20:30-20:45 | 15 | **Module 3** สถาปัตยกรรม Auto Workflow แบบครบวงจร |
| 20:45-21:35 | 50 | **Workshop 3** Group Chat Recorder - อ่านโค้ดและเก็บแชทกลุ่มลงฐานข้อมูล |
| 21:35-22:05 | 30 | **Workshop 4** AI สรุปประชุมกลุ่ม ติดตามงาน และ Admin Dashboard |
| 22:05-22:15 | 10 | พักเบรก |
| 22:15-23:00 | 45 | **Workshop 5 (Capstone)** Executive Daily Report - รายงานอัตโนมัติ |
| 23:00-23:30 | 30 | **Module 4** Codex Cross-check, Governance และแนวทางต่อยอด |

---

## ✅ ตรวจความพร้อมก่อนเริ่ม (Pre-flight Check)

รันชุดคำสั่งนี้ทั้งหมด ทุกข้อต้องผ่านก่อนเข้า Workshop 3

```bash
node -v                     # v22 ขึ้นไป
psql --version              # หรือ docker compose ps ต้องเห็น healthy
ngrok --version             # ต้องรู้จักคำสั่ง
claude --version
```

### 🌿 สลับมาสาขา `day2` ก่อนเป็นอันดับแรก

เมื่อวานเราอยู่ที่สาขา `day1` ซึ่งยังไม่มีฐานข้อมูล **ของทั้งหมดของวันนี้อยู่ที่สาขา `day2`**

```bash
cd line-workflow-app
git checkout day2
git branch --show-current      # ต้องขึ้นว่า day2
```

โค้ดของเมื่อวานยังอยู่ครบทุกบรรทัด สาขา `day2` คือสาขา `day1` ที่ถูก **เติมของเข้าไป** ไม่ใช่เขียนใหม่
อยากให้ผู้เรียนเห็นภาพว่าวันนี้เพิ่มอะไรบ้าง ให้รันคำสั่งนี้เปิดบนจอ

```bash
git diff --stat day1 day2                              # ภาพรวม: ไฟล์ใหม่ 55 ไฟล์ + แก้ของเดิม 9 ไฟล์
git diff --name-status day1 day2 | findstr "^M"        # เฉพาะไฟล์ที่ "แก้ต่อ" จากเมื่อวาน (mac/Linux ใช้ grep)
git diff day1 day2 -- src/routes/webhook.ts            # ไฟล์ที่เห็นการต่อยอดชัดที่สุด
```

ไฟล์ที่ **แก้ต่อจากของเมื่อวาน** มีแค่ 9 ไฟล์ ที่เหลืออีก 55 ไฟล์เป็นของใหม่ล้วน ๆ

| ไฟล์เดิมที่ถูกต่อยอด | วันนี้เติมอะไร |
| --- | --- |
| `src/routes/webhook.ts` | บันทึกข้อความกลุ่มลง DB + ดาวน์โหลดไฟล์ + รองรับ unsend |
| `src/config.ts` | `DATABASE_URL`, ค่าของ Dashboard, ค่าเก็บไฟล์ และตัวตั้งเวลา |
| `src/index.ts` | EJS + session + เส้นทางของ Dashboard + health ที่นับข้อความในฐานข้อมูล |
| `src/line/client.ts` | ดึงชื่อสมาชิกกลุ่ม + ดาวน์โหลดไฟล์จาก LINE (blob client) |
| `src/services/flex.ts` | การ์ดเพิ่มอีก 3 แบบ: สรุปประชุม, เตือนงานค้าง, รายงานยอดขาย |

> 🎓 **จุดสอนที่ใช้เปิดคาบได้ดี:** เปิด `git diff` ของ `webhook.ts` ให้ดูว่าบรรทัดที่เมื่อวานเขียนว่า
> *"(Day 1 ยังไม่เก็บลงฐานข้อมูล - Workshop 3 ของวันที่ 2 จะเพิ่มส่วนนี้)"* กำลังจะถูกแทนที่ด้วยอะไร
> ทั้งวันนี้คือการเติมบรรทัดนั้นให้เต็ม

แล้วทดสอบชุดไฟล์ workshop

```bash
npm install                 # ★ ต้องรันซ้ำหลังสลับสาขา (วันนี้ใช้แพ็กเกจเพิ่มอีก 7 ตัว)
copy .env.example .env      # macOS/Linux ใช้ cp
npm run db:setup            # ต้องขึ้น "พร้อมใช้งาน"
npm run dev                 # เปิด http://localhost:3000 ได้
```

> ⚠️ **ถ้าขึ้น error หา module `pg` / `ejs` / `express-session` ไม่เจอ** แปลว่าลืม `npm install` หลังสลับสาขา
> และถ้ามีไฟล์ `.env` จากเมื่อวานอยู่แล้ว (ไม่ถูกลบเพราะอยู่ใน `.gitignore`) ให้เทียบกับ `.env.example`
> ของสาขานี้แล้วเติมบรรทัดที่ขาด โดยเฉพาะ `DATABASE_URL`

| รายการ | สถานะที่ต้องได้ |
| --- | --- |
| สาขา git | `git branch --show-current` ขึ้นว่า `day2` |
| PostgreSQL 16 | ต่อได้ (ผ่าน Docker หรือติดตั้งลงเครื่อง) |
| ฐานข้อมูล `linechat` | สร้างแล้วและมีตารางครบ 7 ตาราง + 3 view |
| ngrok | รันคำสั่งได้ และผูก authtoken แล้ว (`ngrok config add-authtoken ...`) |
| `/mcp` ใน Claude Code | `line-bot` ยังขึ้น connected จากเมื่อวาน |
| ไลน์กลุ่มทดสอบ | มีสมาชิก 3-4 คนพร้อมแล้ว (ยังไม่เชิญบอท) |
| Dashboard | เปิด http://localhost:3000 ล็อกอิน `admin` / `admin1234` ได้ |

> 💡 **แผนสำรองสำหรับผู้ที่ยังตั้ง PostgreSQL ไม่สำเร็จ:** ใช้ Docker แทน `docker compose up -d` จะได้ PostgreSQL 16 ที่ `postgres://postgres:postgres@localhost:5432/linechat` ทันทีโดยไม่ต้องตั้งค่าอะไรเพิ่ม

---

## 📚 Module 3: สถาปัตยกรรม Auto Workflow แบบครบวงจร

### เวลา 20:30 - 20:45 น.

> 💡 **หัวใจของ Module นี้:** ทั้งวันนี้ตั้งอยู่บนคำถามเดียวคือ "ถ้า API ปลายทางไม่ให้สิ่งที่เราต้องการ เราจะทำอย่างไร" คำตอบคือ **เปลี่ยนจากการดึงเมื่อต้องใช้ (pull) เป็นการรับไว้ตั้งแต่ต้น (push แล้วเก็บ)** หลักคิดนี้ใช้ได้กับทุกระบบ ไม่ใช่แค่ LINE

---

### 3.1 ทบทวนกำแพงที่เจอเมื่อวาน

```
เมื่อวานเราทำได้:                        สิ่งที่ยังทำไม่ได้:

Claude Code ──MCP──► LINE               Claude Code ──?──► ประวัติแชทในกลุ่ม
   push / broadcast / flex                    (ไม่มี API ให้ดึง เลย)
   สร้าง Rich Menu                            (เครื่องมือทั้ง 12 ตัวไม่มีตัวไหนทำได้)
```

> ⚠️ **ข้อเท็จจริงที่ต้องย้ำ:** LINE Messaging API **ไม่มี endpoint ใดที่อ่านประวัติแชทได้เลย** บอทได้รับข้อความผ่าน Webhook เฉพาะข้อความที่เกิด **หลังจาก** บอทเข้ากลุ่มแล้วเท่านั้น นี่ไม่ใช่บั๊กแต่เป็นการออกแบบเพื่อความเป็นส่วนตัว

### 3.2 คำตอบเชิงสถาปัตยกรรม: สร้างหน่วยความจำของเราเอง

```
สมาชิกในกลุ่ม LINE พิมพ์ข้อความ
        │
        ▼
LINE Platform ──POST──► Webhook Server (Express + @line/bot-sdk)
   ผ่าน HTTPS (ngrok)            │ ตรวจ signature แล้วบันทึก
                                 ▼
                        ┌─────────────────────┐
                        │   PostgreSQL 16     │  ◄── หน่วยความจำของระบบ
                        │  line_messages      │
                        │  line_groups        │
                        │  group_tasks        │
                        │  group_summaries    │
                        │  message_logs       │
                        │  sales_orders       │
                        └──────────┬──────────┘
                                   │ อ่านผ่าน Postgres MCP (ขาอ่าน)
                                   ▼
                        Claude Code / GPT Codex
                         วิเคราะห์ + สรุป + สกัดงาน
                                   │ ส่งกลับผ่าน LINE Bot MCP (ขาส่ง)
                                   ▼
                        กลุ่ม LINE เดิม (push message ด้วย groupId)
```

**4 ส่วนที่จะสร้างวันนี้**

| ส่วน | เทคโนโลยี | หน้าที่ | สร้างใน |
| --- | --- | --- | --- |
| 1. Webhook (ขาเข้า) | Node.js + Express + @line/bot-sdk | รับ event จาก LINE แล้วบันทึก | Workshop 3 |
| 2. ฐานข้อมูล (หน่วยความจำ) | PostgreSQL 16 | เก็บข้อความ งาน สรุป ยอดขาย log | Workshop 3 |
| 3. Postgres MCP (ขาอ่าน) | `@modelcontextprotocol/server-postgres` | ให้ AI query ฐานข้อมูลด้วยภาษาไทย | Workshop 4 |
| 4. LINE Bot MCP (ขาส่ง) | `@line/line-bot-mcp-server` | ส่งผลลัพธ์กลับเข้ากลุ่ม | ทำแล้วเมื่อวาน |

### 3.3 แนวคิดสำคัญที่สุดของหลักสูตร: งาน ad-hoc กับงานประจำ

| ลักษณะงาน | ควรใช้ | เหตุผล | ตัวอย่าง |
| --- | --- | --- | --- |
| **งาน ad-hoc** ถามครั้งเดียว คำถามเปลี่ยนทุกครั้ง | สั่ง AI สดผ่าน MCP | ยืดหยุ่นสูง ไม่ต้องเขียนโค้ด | "เมื่อวานกลุ่มคุยเรื่องอะไร" |
| **งานประจำ** ทำซ้ำเวลาเดิมทุกวัน รูปแบบตายตัว | สคริปต์ + ตั้งเวลา | เสถียร ตรวจสอบได้ ไม่กินค่า token | รายงานยอดขายทุกเช้า 08:00 |

> ✅ **และไม่ว่าจะแบบไหน AI Agent คือคนเขียนสคริปต์ให้เรา** เราแค่ต้องรู้ว่าจะสั่งอะไร นี่คือประโยคที่สรุปทั้งหลักสูตรได้ในบรรทัดเดียว

### 3.4 สี่เส้นทางของข้อมูลในระบบเดียวกัน

| เส้นทาง | ใช้เมื่อไหร่ | ใครสั่ง |
| --- | --- | --- |
| LINE -> webhook -> DB | ตลอดเวลาที่มีคนพิมพ์ในกลุ่ม | อัตโนมัติ |
| DB -> Postgres MCP -> AI Agent | ถามอะไรก็ได้แบบ ad-hoc | คนพิมพ์ prompt |
| DB -> Dashboard -> LINE | งานประจำวันที่ต้องกดดูและตัดสินใจ | admin กดปุ่ม |
| DB -> สคริปต์ -> LINE | งานประจำเวลาเดิมทุกวัน | Task Scheduler |

> 🎓 **จุดสอนที่สำคัญ:** ทั้ง 4 เส้นทาง **ใช้ฐานข้อมูลเดียวกัน** สร้างครั้งเดียวได้ประโยชน์หลายทาง นี่คือเหตุผลว่าทำไม "หน่วยความจำกลาง" สำคัญกว่าการเขียนบอทเก่ง ๆ ตัวเดียว

---

## 🛠️ Workshop 3: Group Chat Recorder - เก็บแชทกลุ่มลงฐานข้อมูล

### เวลา 20:45 - 21:35 น. (50 นาที)

> **บทบาทที่สวม:** เจ้าหน้าที่ IT ที่สร้างระบบให้ทีมปฏิบัติการ
> **โจทย์:** ทำให้ทุกข้อความในไลน์กลุ่มทีมไหลเข้าฐานข้อมูลโดยอัตโนมัติ เพื่อให้ AI นำไปวิเคราะห์ต่อได้

---

### Part A: เตรียมกลุ่มทดสอบและเปิดสิทธิ์ให้บอท (7 นาที)

#### A1. เปิดให้บอทเข้ากลุ่มได้ (ค่าเริ่มต้นปิดอยู่)

1. เข้า https://manager.line.biz/ เลือก OA ของคุณ
2. **Settings > Account settings > Chats**
3. เปิด **"Allow bot to join group chats"**
4. **Settings > Response settings** ตรวจว่า
   - Auto-response messages: **ปิด**
   - Webhook: **เปิด**

> ⚠️ **ถ้าไม่เปิดข้อ 3 จะเชิญบอทเข้ากลุ่มไม่ได้เลย** และจะหาสาเหตุไม่เจอเพราะไม่มี error ขึ้น แค่หาบอทไม่พบตอนค้นหา

#### A2. สร้างไลน์กลุ่มทดสอบและเชิญบอท

- จับกลุ่ม 3-4 คน ตั้งชื่อกลุ่ม เช่น `ทดสอบ LINE Workflow - กลุ่ม 1`
- เชิญบอทเข้ากลุ่มด้วย **Bot basic ID** (เช่น `@123abcd` ดูได้จาก tab Messaging API)

> ⚠️ **ยังไม่ต้องพิมพ์อะไรในกลุ่ม** รอ webhook พร้อมก่อน เพราะข้อความที่พิมพ์ตอนนี้จะหายไปเปล่า ๆ (บอทได้รับ event แต่ยังไม่มีเซิร์ฟเวอร์รอรับ)

#### A3. แจ้งความยินยอม (ทำจริงทุกครั้ง ไม่ใช่แค่ในคลาส)

พิมพ์ในกลุ่มว่า

> "กลุ่มนี้ใช้ทดสอบระบบบันทึกบทสนทนาสำหรับการอบรม ข้อความจะถูกเก็บลงฐานข้อมูลของผู้เรียน"

> ⚠️ **หลักปฏิบัติด้าน PDPA:** ในระบบจริงต้องได้รับความยินยอมจากสมาชิกทุกคนก่อนเสมอ ระบบที่เราสร้างจะให้บอทประกาศตัวเองตอนถูกเชิญเข้ากลุ่มอยู่แล้ว (ดูโค้ด event `join` ใน Part C) และในฐานข้อมูลมีคอลัมน์ `consent_at` ในตาราง `line_groups` ไว้บันทึกว่าแจ้งเมื่อไหร่

---

### Part B: สร้างตารางในฐานข้อมูล (5 นาที)

```bash
psql -U postgres -c "CREATE DATABASE linechat;"
```

**Schema หลักที่ใช้เก็บข้อความ** (มีให้แล้วใน `sql/01_schema.sql`)

```sql
CREATE TABLE IF NOT EXISTS line_messages (
    id              SERIAL PRIMARY KEY,
    line_message_id VARCHAR(64) UNIQUE,        -- กัน insert ซ้ำเมื่อ LINE retry webhook
    group_id        VARCHAR(64),               -- ID ของกลุ่ม (ขึ้นต้นด้วย C)
    user_id         VARCHAR(64),               -- ID ของผู้ส่ง (ขึ้นต้นด้วย U)
    display_name    VARCHAR(255),              -- ชื่อผู้ส่ง ณ เวลาที่ส่ง
    message_type    VARCHAR(20) NOT NULL,      -- text / sticker / image / video / file
    message_text    TEXT,                      -- เนื้อความ (เฉพาะ message_type = 'text')
    sent_at         TIMESTAMPTZ NOT NULL,      -- เวลาส่งจริงจาก LINE (event.timestamp)
    created_at      TIMESTAMPTZ DEFAULT now()  -- เวลาที่บันทึกลงฐานข้อมูล
);

CREATE INDEX IF NOT EXISTS idx_msg_group_time ON line_messages (group_id, sent_at DESC);
CREATE INDEX IF NOT EXISTS idx_msg_user       ON line_messages (user_id);
CREATE INDEX IF NOT EXISTS idx_msg_sent_at    ON line_messages (sent_at DESC);
```

**สี่จุดออกแบบสำคัญที่ต้องอธิบายให้ได้** (ข้อสอบสัมภาษณ์งานจริงถามเรื่องนี้บ่อย)

| จุดออกแบบ | เหตุผล |
| --- | --- |
| `line_message_id UNIQUE` | LINE ส่ง event ซ้ำได้เมื่อไม่ได้รับ 200 ทันเวลา ต้องกันซ้ำที่ **ระดับฐานข้อมูล** ไม่ใช่ระดับโค้ด |
| เก็บ `display_name` ไว้ในแถว | ชื่อคนเปลี่ยนได้ ต้องเก็บชื่อ **ณ เวลาที่พูด** ไม่ใช่ join จากตารางอื่นทีหลัง (ไม่งั้นรายงานเก่าจะเปลี่ยนชื่อตามไปด้วย) |
| `sent_at` แยกจาก `created_at` | เวลาที่พูด vs เวลาที่เราบันทึก ต่างกันได้ เช่นตอนระบบล่มแล้ว LINE retry เข้ามาทีหลัง |
| `TIMESTAMPTZ` ไม่ใช่ `TIMESTAMP` | LINE ส่ง epoch เป็น UTC ถ้าเก็บผิดชนิด รายงาน "ของวันนี้" จะเพี้ยน **7 ชั่วโมง** ในไทย |

**ตารางทั้งหมดในระบบ (7 ตาราง + 3 view)**

```
line_groups (group_id PK)
     │ 1
     ├──── * line_messages   (group_id, line_message_id UNIQUE, sent_at)
     ├──── * group_tasks     (group_id, assignee, task_text, status, source)
     ├──── * group_summaries (group_id, summary_date, summary_text, topics jsonb)
     └──── * media_files     (line_message_id UNIQUE, storage_key, caption, status)

message_logs  (ขาส่งทั้งหมด: target_type/target_id, message_kind, status, sent_by)
sales_orders  (ข้อมูลธุรกิจจำลองสำหรับ Capstone)

view v_messages_today   ข้อความของวันนี้พร้อมชื่อกลุ่ม (ให้ AI query ง่าย)
view v_daily_sales      สรุปยอดขายรายวัน/สาขา/ช่องทาง
view v_media_gallery    ไฟล์ที่เก็บไว้พร้อมชื่อกลุ่ม (เฉพาะที่ status = stored)
```

> **หมายเหตุ:** `media_files` กับ `v_media_gallery` เป็นของ **[Workshop 6 (โมดูลเสริม) เรื่องเก็บรูปและไฟล์เอกสารจากกลุ่ม](#-workshop-6-โมดูลเสริม-เก็บรูปและไฟล์เอกสารจากกลุ่มไว้ที่เซิร์ฟเวอร์ของเรา)**
> `npm run db:setup` สร้างให้ทุกครั้งอยู่แล้ว ถ้าไม่ได้สอน Workshop 6 ก็ปล่อยว่างไว้ได้ ไม่กระทบอะไร
> ส่วนตาราง `user_sessions` จะถูกสร้างอัตโนมัติโดย `connect-pg-simple` เมื่อเปิด `SESSION_IN_DATABASE=true`
> (ไม่ได้อยู่ในไฟล์ schema)

> 💡 **เคล็ดลับสำหรับ MCP:** การสร้าง `VIEW` ที่ join และ filter ไว้ให้แล้ว ช่วยให้ AI เขียน SQL ถูกตั้งแต่ครั้งแรกมากขึ้น และยังใช้ **จำกัดขอบเขตข้อมูลที่ AI มองเห็น** ได้ด้วย (ให้สิทธิ์เฉพาะ view ไม่ให้ตารางดิบ)

รันจริง:

```bash
npm run db:setup
```

---

### Part C: อ่านโค้ด Webhook Server จากชุดไฟล์ที่ clone มา (25 นาที)

> 🎓 **แนวทางของ Part นี้:** เราไม่สั่ง AI เขียนใหม่จากศูนย์ เพราะเวลาในคลาสสั้นและผลลัพธ์ที่ AI เขียนจะไม่เหมือนกันทุกคน จนพากันไปติดคนละจุด **เราจะอ่านโค้ดจริงที่ทำงานได้แล้วในสาขา `day2` แทน** ซึ่งได้ประโยชน์มากกว่า คือผู้เรียนเห็น "โค้ดที่ถูก" ก่อน แล้วค่อยรู้ว่าทำไมมันถูก

#### C1. เปิดไฟล์หลัก 4 ไฟล์ที่ต้องรู้จัก

```bash
code .        # หรือเปิดด้วย editor ที่ถนัด
```

| ไฟล์ | หน้าที่ | จุดที่ต้องดู |
| --- | --- | --- |
| `src/routes/webhook.ts` | รับ event จาก LINE | การตอบ 200, การตรวจ signature, การแยก event type |
| `src/db.ts` | คุยกับ PostgreSQL | `saveMessage()` และการกันข้อมูลซ้ำ |
| `src/line/client.ts` | เรียก LINE API | `getGroupMemberProfile()` ดึงชื่อผู้ส่ง |
| `src/config.ts` | อ่านค่าจาก `.env` | ค่าที่ต้องกรอกจริงมีอะไรบ้าง |

**กรอกค่าจริงใน `.env` ก่อนรัน**

```bash
copy .env.example .env      # macOS/Linux ใช้ cp
```

ค่าที่ต้องมีจริงในวันนี้คือ `CHANNEL_ACCESS_TOKEN`, `CHANNEL_SECRET`, `DATABASE_URL` และ `PORT` (ค่าเริ่มต้น 3000)

> ⚠️ **Channel secret อยู่ที่ tab Basic settings ไม่ใช่ tab Messaging API** (คนละค่ากับ access token) ถ้าใส่ผิดจะขึ้น `signature validation failed`

#### C2. สิ่งที่ Webhook Server ตัวนี้ทำได้ (ไล่ดูทีละข้อในโค้ด)

| ความสามารถ | ดูที่ไหน |
| --- | --- |
| `POST /webhook` ตรวจ signature ด้วย channel secret ผ่าน middleware ของ `@line/bot-sdk` | `src/routes/webhook.ts` |
| สนใจเฉพาะ event `message` ที่มาจากกลุ่ม (`source.type === 'group'`) | `handleEvent()` |
| ข้อความ text ดึงชื่อผู้ส่งด้วย `getGroupMemberProfile` แล้วบันทึกลงตาราง `line_messages` | `src/line/client.ts` + `src/db.ts` |
| type อื่น (sticker, image) บันทึกเฉพาะ metadata ไม่มี `message_text` (การดึงตัวไฟล์จริงอยู่ใน Workshop 6) | `handleEvent()` |
| กันข้อมูลซ้ำด้วย `ON CONFLICT (line_message_id) DO NOTHING` | `saveMessage()` |
| `GET /health` ตอบ `{ status: 'ok', messagesStored: จำนวนแถว }` | `src/index.ts` |
| event `join` reply ข้อความแนะนำตัวและแจ้งเรื่องการบันทึก | `handleEvent()` |
| event `leave` log ว่าออกจากกลุ่มแล้ว | `handleEvent()` |

#### C3. กฎเหล็ก 3 ข้อของ Webhook LINE

ก่อนไล่อ่านโค้ด ให้จำ 3 ข้อนี้ให้ขึ้นใจ แล้วหาให้เจอว่าโค้ดทำตามแต่ละข้ออย่างไร

```
กฎข้อ 1: ต้องตอบ 200 เสมอ
         ถ้าตอบ 500 ซ้ำ ๆ LINE จะปิด webhook ให้อัตโนมัติ

กฎข้อ 2: ต้องตรวจ signature ด้วย channel secret
         กันคนอื่นยิง request ปลอมเข้ามาที่ endpoint ของเรา

กฎข้อ 3: ห้ามใช้ express.json() ครอบ route /webhook
         signature คำนวณจาก raw body ถ้ามีตัวแปลง JSON มาก่อน จะพังทันที
```

**โค้ดจริงจากชุด workshop (`src/routes/webhook.ts`) ที่ทำถูกทั้ง 3 ข้อ**

```typescript
export function createWebhookRouter(): Router {
  const router = asyncRouter()

  // ถ้ายังไม่ได้ตั้ง CHANNEL_SECRET (เช่นตอนเรียนช่วงแรก) ให้รับ JSON ตรง ๆ
  // และเตือนใน log ว่าข้ามการตรวจ signature อยู่ - ห้ามใช้แบบนี้ใน production
  const verifier = config.channelSecret
    ? middleware({ channelSecret: config.channelSecret })
    : express.json()

  router.post('/webhook', verifier, async (req: Request, res: Response) => {
    // ตอบ 200 ทันที ไม่ให้ LINE รอ  <-- กฎข้อ 1
    res.status(200).end()

    const events: WebhookEvent[] = req.body?.events ?? []
    for (const event of events) {
      try {
        await handleEvent(event)          // try/catch หุ้มทีละ event
      } catch (err) {
        console.error('[Webhook] ประมวลผล event ไม่สำเร็จ:', err instanceof Error ? err.message : err)
      }
    }
  })

  return router
}
```

> 🎓 **จุดสอน:** สังเกตว่า `res.status(200).end()` ถูกเรียก **ก่อน** ประมวลผล event ทั้งหมด นี่ไม่ใช่ความมักง่าย แต่เป็นการออกแบบที่ถูกต้อง LINE ต้องได้ 2xx กลับภายในราว 2 วินาที ส่วนงานหนัก (query DB, เรียก API ดึงชื่อ) ให้ทำทีหลัง และ `try/catch` หุ้มทีละ event ทำให้ event เดียวที่พังไม่ทำให้ event อื่นไม่ถูกประมวลผล

**การจัดการ event `join` ที่แสดงความรับผิดชอบด้าน PDPA**

```typescript
// บอทถูกเชิญเข้ากลุ่ม -> แนะนำตัว + ขอความยินยอม (สำคัญเรื่อง PDPA)
if (event.type === 'join' && event.source.type === 'group') {
  const groupId = event.source.groupId
  const memberCount = await getGroupMemberCount(groupId)
  await upsertGroup(groupId, { memberCount, isActive: true })
  await replyText(
    event.replyToken,
    [
      'สวัสดีครับ ผมคือบอทบันทึกบทสนทนาของทีม',
      'ผมจะเริ่มบันทึกข้อความในกลุ่มนี้ตั้งแต่ตอนนี้เป็นต้นไป เพื่อใช้สรุปประชุมและติดตามงาน',
      'ข้อความก่อนหน้านี้ผมมองไม่เห็นนะครับ',
      'ถ้าไม่สะดวกให้บันทึก แจ้งผู้ดูแลเพื่อเชิญผมออกจากกลุ่มได้เลยครับ'
    ].join('\n')
  )
  return
}
```

**การบันทึกข้อความที่กันซ้ำถูกต้อง**

```typescript
export async function saveMessage(record: MessageRecord): Promise<'inserted' | 'skipped' | 'error'> {
  const sql = `
    INSERT INTO line_messages
      (line_message_id, group_id, user_id, display_name, message_type, message_text, sent_at)
    VALUES ($1, $2, $3, $4, $5, $6, $7)
    ON CONFLICT (line_message_id) DO NOTHING`

  try {
    const result = await pool.query(sql, [/* ...ค่าทั้ง 7 ... */])
    const who = record.displayName ?? record.userId ?? 'ไม่ทราบผู้ส่ง'

    // rowCount = 0 แปลว่าชนกับข้อมูลเดิม ถูกข้ามด้วย ON CONFLICT DO NOTHING
    if (!result.rowCount) {
      console.log(`[ข้าม] ข้อความซ้ำ id=${record.lineMessageId} จาก ${who}`)
      return 'skipped'
    }

    console.log(`[บันทึก] ${who} (${record.messageType}): ${record.messageText ?? `[${record.messageType}]`}`)
    return 'inserted'
  } catch (err) {
    // DB พังก็แค่ log ไว้ ไม่ throw เพราะ webhook ตอบ 200 ไปแล้ว
    console.error('[DB] บันทึกข้อความไม่สำเร็จ:', err instanceof Error ? err.message : err)
    return 'error'
  }
}
```

> ⚠️ **เกร็ดเชิงเทคนิคที่พบจากการทดสอบจริง:** `ON CONFLICT DO NOTHING` คู่กับ `RETURNING` **จะไม่คืนแถวเมื่อชนกัน** ดังนั้นต้องเช็คด้วย `result.rowCount` จึงจะแยกได้ว่า "บันทึกใหม่" หรือ "ข้ามเพราะซ้ำ"

#### C4. 6 คำถามตรวจโค้ด (ขั้นตอนที่คนมักข้าม)

ให้ผู้เรียนหาคำตอบ **จากโค้ดจริง** ทีละข้อ บอกได้ด้วยว่าอยู่ไฟล์ไหน บรรทัดประมาณไหน

| # | คำถาม | คำตอบที่ควรได้ และดูที่ไหน |
| --- | --- | --- |
| 1 | ถ้าฐานข้อมูลล่ม `/webhook` ยังตอบ 200 หรือไม่ | ตอบ เพราะ `res.status(200).end()` ถูกเรียกก่อนแตะ DB (`src/routes/webhook.ts`) |
| 2 | ถ้า signature ไม่ถูกต้อง จะตอบ status อะไร | 401 จาก middleware ของ `@line/bot-sdk` ก่อนเข้า handler ของเรา |
| 3 | `express.json()` ถูกวางไว้ครอบ `/webhook` หรือเปล่า | ไม่ ใช้เฉพาะตอนที่ยังไม่ได้ตั้ง `CHANNEL_SECRET` เท่านั้น (ดูตัวแปร `verifier`) |
| 4 | ถ้า `getGroupMemberProfile` ล้มเหลว ข้อความยังถูกบันทึกอยู่ไหม | ยังบันทึก แต่ `display_name` จะเป็น null (`src/line/client.ts`) |
| 5 | ถ้า LINE ส่ง event เดิมซ้ำ จะเกิดข้อมูลซ้ำในตารางหรือไม่ | ไม่ เพราะ `ON CONFLICT (line_message_id) DO NOTHING` (`saveMessage()`) |
| 6 | มีการ log ค่า token หรือ secret ออก console ที่ไหนบ้าง | ไม่มี ลองค้นด้วย `grep -rn "CHANNEL_ACCESS_TOKEN" src/` เพื่อยืนยัน |

> 🎓 **จุดสอนที่สำคัญมาก:** 6 ข้อนี้คือ **checklist ที่ใช้ตรวจโค้ดของใครก็ได้** ไม่ว่าจะเป็นโค้ดที่ AI เขียน เพื่อนร่วมทีมเขียน หรือโค้ดเก่าในบริษัท ทักษะที่แท้จริงไม่ใช่การเขียนโค้ดให้เร็ว แต่คือ **การรู้ว่าต้องตรวจอะไรบ้าง** เพราะเราคือคนที่รู้ว่าอะไรสำคัญในบริบทของเรา
>
> 💡 **ต่อยอดในคลาสได้ถ้าเวลาเหลือ:** ให้ผู้เรียนเปิด `claude` ในโฟลเดอร์โปรเจกต์ แล้วถามด้วย 6 ข้อนี้ เพื่อเทียบว่าคำตอบของ AI ตรงกับที่ตัวเองอ่านเจอหรือไม่ ตรงไหนที่ AI ตอบผิดคือจุดที่น่าสนใจที่สุด

#### C5. เปิด Webhook สู่โลกภายนอกด้วย ngrok

```bash
npm run dev              # terminal 1
ngrok http 3000          # terminal 2
```

ngrok จะพิมพ์บรรทัด `Forwarding` ออกมา เช่น `https://xxxx-yyyy-zzzz.ngrok-free.app -> http://localhost:3000`

จากนั้น
1. ไปที่ LINE Developers Console > tab **Messaging API** > **Webhook URL**
2. ใส่ `https://xxxx-yyyy-zzzz.ngrok-free.app/webhook` (อย่าลืม `/webhook` ต่อท้าย)
3. กด **Verify** ต้องขึ้น Success
4. เปิดสวิตช์ **Use webhook**

> ⚠️ **ข้อควรรู้ 2 ข้อ**
> 1. URL ของ ngrok **เปลี่ยนทุกครั้งที่ปิดแล้วเปิดใหม่** ต้องกลับมาแก้ใน Console ทุกครั้ง ดังนั้น **เปิด ngrok ทิ้งไว้ตลอดคาบ อย่าปิด**
> 2. ถ้ายังไม่ได้ผูก authtoken จะรันไม่ผ่าน ให้รัน `ngrok config add-authtoken <AUTHTOKEN_ของคุณ>` หนึ่งครั้งก่อน (ดู token ที่ https://dashboard.ngrok.com)

**เครื่องมือช่วยดีบักที่ควรเปิดคู่กันตลอดคาบ**

เปิด http://127.0.0.1:4040 ในเบราว์เซอร์ จะเห็นทุก request ที่ LINE ยิงเข้ามา ทั้ง header (`x-line-signature`) และ body แบบ JSON เต็ม ๆ
ถ้ากด Verify ไม่ผ่าน ให้ดูหน้านี้ก่อนเสมอ ว่า request มาถึงเครื่องเราจริงหรือไม่ และเราตอบ status อะไรกลับไป

#### C6. ทดสอบเก็บข้อความจริง (10 นาที) - ช่วงที่สนุกที่สุด

ให้สมาชิกในกลุ่มคุยกันจริงในหัวข้อจำลอง **"วางแผนงานทีมปฏิบัติการสัปดาห์นี้"** ให้ได้ 25-30 ข้อความ และ **ต้องมีองค์ประกอบเหล่านี้** เพราะจำเป็นสำหรับ Workshop 4

| องค์ประกอบ | ตัวอย่างประโยค | ใช้ทดสอบอะไร |
| --- | --- | --- |
| มอบหมายงานชัดเจน | "ฝากนภาทำใบขอซื้อภายในวันนี้" | AI สกัดงานได้ไหม |
| รับปาก | "รับทราบครับ ผมจะสรุปยอดส่งให้พรุ่งนี้" | AI จับผู้รับผิดชอบถูกไหม |
| นัดหมาย | "ประชุมจันทร์ 9 โมง" | AI จับกำหนดเวลาได้ไหม |
| เรื่องที่ตกลงไม่ได้ | "เรื่องงบยังไม่สรุป รอพี่เอกเคาะ" | AI สรุปประเด็นค้างเป็นไหม |
| sticker 2-3 ตัว | (ส่งสติกเกอร์) | ระบบเก็บ metadata ถูกไหม |

ตรวจใน terminal ว่ามี log `[บันทึก]` ไหลเข้า และตรวจในฐานข้อมูล

```bash
psql -U postgres -d linechat -c "SELECT display_name, message_type, message_text, sent_at FROM line_messages ORDER BY sent_at DESC LIMIT 10;"
```

#### C7. หา groupId เก็บไว้ใช้ส่งกลับ

```bash
psql -U postgres -d linechat -c "SELECT DISTINCT group_id FROM line_messages;"
```

จด groupId (ขึ้นต้นด้วย `C`) ไว้ ใช้ทั้งใน Workshop 4 และ 5

---

### Part D: แผนสำรอง ถ้าไม่มีกลุ่มจริงหรือเชิญบอทไม่สำเร็จ

ชุดไฟล์ workshop มีทางเลือกให้เรียนต่อได้ทันที ไม่ต้องรอแก้ปัญหา LINE

```bash
npm run seed:chat       # ใส่บทสนทนากลุ่มจำลอง 164 ข้อความ ใน 3 กลุ่ม
npm run dev
```

เปิด http://localhost:3000/simulator แล้วพิมพ์บทสนทนาเพิ่มเองได้ (บรรทัดละ 1 ข้อความ) ข้อมูลจะลงตาราง `line_messages` เหมือนมาจาก LINE จริง ทำ Workshop 4-5 ต่อได้ครบทุกขั้น

---

### 🧯 เกร็ดสำคัญที่ต้องย้ำในคลาส (5 ข้อ)

1. **บอทมองข้อความก่อนเข้ากลุ่มไม่เห็น** - คำถามยอดฮิตคือ "ทำไมข้อความเมื่อวานไม่เข้า" คำตอบอยู่ที่นี่
2. **ต้องตอบ 200 เสมอ** - ถ้าตอบ 500 ซ้ำ ๆ LINE จะปิด webhook ให้อัตโนมัติ ต้องกด Verify ใหม่
3. **signature ต้องคำนวณจาก raw body** - ถ้าใส่ `express.json()` ก่อน middleware จะพังทันที
4. **`userId` ไม่จำเป็นต้องมี** - ถ้าผู้ใช้ปิดการแชร์โปรไฟล์ `source.userId` จะเป็น `undefined` โค้ดต้องรับมือได้
5. **ห้าม commit `.env`** - Channel access token คือกุญแจของ OA ทั้งบัญชี

### ✅ Checkpoint Workshop 3

- [ ] `curl http://localhost:3000/health` ตอบ `{"status":"ok",...}`
- [ ] Verify webhook ใน Console ขึ้น Success
- [ ] พิมพ์ในกลุ่มแล้วเห็น log `[บันทึก]` ใน terminal
- [ ] `SELECT COUNT(*) FROM line_messages` ได้เลขที่เพิ่มขึ้นจริง
- [ ] จด groupId ไว้แล้ว
- [ ] อธิบายได้ว่าทำไม webhook ต้องตอบ 200 เสมอ
- [ ] ชี้ได้ว่ากฎเหล็ก 3 ข้ออยู่ตรงไหนใน `src/routes/webhook.ts`

---

## 🛠️ Workshop 4: AI สรุปประชุมกลุ่ม ติดตามงาน และ Admin Dashboard

### เวลา 21:35 - 22:05 น. (30 นาที)

> **โจทย์:** ตอนนี้ข้อมูลอยู่ในฐานข้อมูลแล้ว ถึงเวลาให้ AI อ่านและวิเคราะห์ แล้วส่งผลลัพธ์กลับเข้ากลุ่มให้ทุกคนเห็นพร้อมกัน

---

### Part A: เชื่อม Postgres MCP (5 นาที)

**Windows (Command Prompt)**

```cmd
claude mcp add postgres-linechat ^
  -- npx -y @modelcontextprotocol/server-postgres postgresql://postgres:รหัสผ่าน@localhost:5432/linechat
```

macOS / Linux เปลี่ยน `^` เป็น `\`

ตรวจด้วย `/mcp` ต้องเห็นทั้ง `line-bot` และ `postgres-linechat` เป็น **connected**

> ⚠️ **แนวปฏิบัติสำคัญสำหรับระบบจริง: ใช้ read-only user เท่านั้น**
>
> ในระบบจริงห้ามให้ MCP ต่อด้วย user `postgres` เด็ดขาด เพราะ AI จะลบตารางได้ ให้สร้าง user แบบอ่านอย่างเดียว
>
> ```sql
> CREATE USER ai_reader WITH PASSWORD 'xxxx';
> GRANT CONNECT ON DATABASE linechat TO ai_reader;
> GRANT USAGE ON SCHEMA public TO ai_reader;
> GRANT SELECT ON ALL TABLES IN SCHEMA public TO ai_reader;
> ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO ai_reader;
> ```
>
> **หลักคิด:** ให้สิทธิ์น้อยที่สุดเท่าที่ทำงานได้ (Principle of Least Privilege) ไม่ใช่เพราะไม่ไว้ใจ AI แต่เพราะระบบที่ดีต้องจำกัดความเสียหายเมื่อเกิดข้อผิดพลาด

---

### Part B: 6 prompt วิเคราะห์บทสนทนา (15 นาที)

ทำตามลำดับ ยากขึ้นเรื่อย ๆ

#### B1. สรุปว่าคุยเรื่องอะไร

```
อ่านข้อความทั้งหมดในตาราง line_messages ของวันนี้
แล้วสรุปว่ากลุ่มคุยเรื่องอะไรกันบ้าง แบ่งเป็นหัวข้อ
พร้อมระบุว่าใครเป็นคนเสนอประเด็นไหน
ตอบเป็นภาษาไทย ไม่เกิน 15 บรรทัด
```

#### B2. วิเคราะห์พฤติกรรมกลุ่ม

```
วิเคราะห์จากตาราง line_messages เฉพาะกลุ่ม <ใส่ groupId ของคุณ>
1. ใครพูดเยอะสุด 3 อันดับ (จำนวนข้อความและสัดส่วนเปอร์เซ็นต์)
2. ช่วงเวลาไหนของวันที่กลุ่มคุยกันคึกคักที่สุด
3. ความยาวข้อความเฉลี่ยของแต่ละคน บอกอะไรเกี่ยวกับสไตล์การสื่อสารบ้าง
แสดง SQL ที่ใช้ให้ผมดูด้วย
```

> 🎓 **จุดสอน:** ประโยค **"แสดง SQL ที่ใช้ให้ผมดูด้วย"** เป็นทั้งการตรวจงานและการเรียนรู้ในตัว การอ่าน SQL ที่ AI เขียนคือวิธีเรียน SQL ที่เร็วที่สุด เพราะเป็นการอ่านของคนที่เขียนเก่ง พร้อมบริบทที่เราเข้าใจอยู่แล้ว

**ตัวอย่าง SQL ที่ AI ควรเขียนออกมาสำหรับข้อ 1**

```sql
SELECT display_name,
       COUNT(*) AS message_count,
       ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS percent
  FROM line_messages
 WHERE group_id = 'Cxxxxxxxx'
   AND (sent_at AT TIME ZONE 'Asia/Bangkok')::date = (now() AT TIME ZONE 'Asia/Bangkok')::date
 GROUP BY display_name
 ORDER BY message_count DESC
 LIMIT 3;
```

> ⚠️ **จุดที่ต้องตรวจทุกครั้ง:** มี `AT TIME ZONE 'Asia/Bangkok'` หรือไม่ ถ้าไม่มี คำว่า "วันนี้" จะเพี้ยนไป 7 ชั่วโมง ข้อความช่วงเช้าตรู่จะถูกนับเป็นของเมื่อวาน

#### B3. ติดตามงาน (ใช้งานจริงในทีมได้ทันที)

```
ค้นในบทสนทนาของกลุ่ม <groupId> วันนี้ ว่ามีการนัดหมาย มอบหมายงาน
หรือมีใครรับปากว่าจะทำอะไรบ้าง
สรุปเป็นตาราง: ใครรับผิดชอบ / งานอะไร / กำหนดเมื่อไหร่ (ถ้าระบุ) / ข้อความต้นทาง
เอาเฉพาะที่ชัดเจน ห้ามเดาเอง ถ้าไม่ชัดให้ระบุว่า "ไม่ชัดเจน"
```

> 🎓 **จุดสอน:** วรรค **"ห้ามเดาเอง ถ้าไม่ชัดให้ระบุว่าไม่ชัดเจน"** สำคัญมากในงานจริง เพราะ AI มีแนวโน้มจะเติมช่องว่างให้ดูสมบูรณ์ การบังคับให้แยก "ที่รู้" ออกจาก "ที่เดา" ทำให้ผลลัพธ์เอาไปใช้ตัดสินใจได้จริง

#### B4. หาประเด็นที่ยังตกลงกันไม่ได้

```
จากบทสนทนาวันนี้ มีเรื่องอะไรที่คุยแล้วยังไม่ได้ข้อสรุป หรือรอการตัดสินใจจากใครอยู่
เรียงตามความเร่งด่วน พร้อมบอกว่าใครควรเป็นคนตัดสินใจ
```

#### B5. สรุปแล้วส่งกลับเข้ากลุ่ม (ไฮไลท์ของ Workshop)

```
สรุปบทสนทนาวันนี้ของกลุ่ม <groupId> ให้กระชับไม่เกิน 10 บรรทัด
ขึ้นต้นว่า "สรุปประชุมกลุ่มวันนี้" ตามด้วยประเด็นสำคัญเป็นข้อ ๆ
และปิดท้ายด้วยรายการงานที่มีคนรับไปทำ
ห้ามใช้ markdown เพราะ LINE ไม่รองรับ
แสดงข้อความให้ผมอ่านยืนยันก่อน แล้วจึงส่งเข้ากลุ่มด้วย push_text_message
```

> ✅ **นี่คือ moment ของวันที่สอง** เมื่อสมาชิกทุกคนในกลุ่มเห็นสรุปประชุมที่ AI ทำให้โผล่ขึ้นมาในไลน์พร้อมกัน ระบบครบวงจรแล้ว ทั้งขารับ ขาวิเคราะห์ และขาส่งกลับ

#### B6. Flex Message รายงานสวยงาม

```
สร้างสรุปสถิติกลุ่มวันนี้เป็น Flex Message แบบ bubble ขนาด mega
- ส่วนหัวพื้นหลังสีน้ำเงินเข้ม #0C1628 ตัวอักษรขาว หัวข้อ "สรุปประชุมกลุ่มวันนี้"
- แสดงจำนวนข้อความรวม
- Top 3 คนที่คุยเยอะสุด (ชื่อและจำนวน)
- ประเด็นเด่น 3 เรื่อง
- รายการงานที่มีคนรับไปทำ
- footer ตัวเล็กสีเทาว่า "สร้างอัตโนมัติโดย AI Agent"
altText: "สรุปประชุมกลุ่มวันนี้"
แสดง JSON ให้ผมตรวจก่อน แล้วส่งเข้ากลุ่มด้วย push_flex_message
```

**เทียบกับโค้ดจริงในชุด workshop** เปิดไฟล์ `src/services/flex.ts` ดูฟังก์ชัน `flexDailySummary()`

```typescript
/** รายงานสรุปแชทกลุ่มประจำวัน (Workshop 4) */
export function flexDailySummary(input: {
  groupName: string
  dateLabel: string
  messageCount: number
  topTalkers: { name: string; count: number }[]
  topics: string[]
  tasks: { assignee: string; task: string; due?: string }[]
}) {
  // แปลงหัวข้อเป็นรายการมีเลขกำกับ
  const topicItems = input.topics.slice(0, 5).map((t, i) => ({
    type: 'text',
    text: `${i + 1}. ${t}`,
    size: 'sm',
    wrap: true,
    color: '#111827',
    margin: 'xs'
  }))
  // ... ประกอบเป็น bubble ต่อ
}
```

> 💡 **จุดสอน:** สังเกตว่าฟังก์ชันรับ **input เป็น object ที่มี type ชัดเจน** ไม่ใช่รับ JSON ทั้งก้อน ทำให้เรียกใช้ซ้ำได้จากหลายที่ (dashboard, สคริปต์, scheduler) และ TypeScript ช่วยจับข้อผิดพลาดตั้งแต่ตอน compile

---

### Part C: Admin Dashboard - เมื่อผู้ใช้ไม่ใช่ Developer (10 นาที)

> 🎓 **ความจริงที่ต้องยอมรับ:** prompt เก่งแค่ไหนก็ตาม **ผู้จัดการฝ่ายไม่พิมพ์ prompt** เขาต้องการหน้าเว็บที่กดได้ ชุด workshop จึงมี Admin Dashboard ให้เป็นตัวอย่างระบบที่ส่งมอบได้จริง

```bash
npm run dev
```

เปิด http://localhost:3000 ล็อกอิน `admin` / `admin1234`

#### หน้าจอทั้ง 9 หน้าและใช้ทำอะไร

| หน้า | URL | ทำอะไรได้ | ตรงกับ prompt ข้อไหน |
| --- | --- | --- | --- |
| ภาพรวม | `/` | KPI, กราฟข้อความรายวัน 14 วัน, ความคึกคักตามชั่วโมง, Top talkers, โควต้าคงเหลือ | B2 |
| ประวัติแชท | `/messages` | ค้นหาคำ กรองผู้ส่ง/ช่วงวันที่/ชนิด แบ่งหน้า export CSV | B1 |
| AI สรุปบทสนทนา | `/summaries` | เลือกกลุ่มและวันที่ กดสรุป เก็บประวัติ ส่งสรุปกลับเข้ากลุ่มเป็น Flex | B5, B6 |
| ติดตามงาน | `/tasks` | งานที่ AI สกัดมา + เพิ่มเอง ปิดงาน ส่งการ์ดเตือนงานค้างเข้ากลุ่ม | B3 |
| ส่งข้อความ | `/send` | ส่ง text หรือ Flex (เทมเพลตประกาศ หรือวาง JSON เอง) เข้ากลุ่ม/broadcast + dry-run | Workshop 1 |
| รายงานผู้บริหาร | `/sales` | ยอดขายรายวัน สาขา ช่องทาง สินค้าขายดี รายการผิดปกติ ส่ง Flex | Workshop 5 |
| จัดการกลุ่ม | `/groups` | ตั้งชื่อกลุ่ม บันทึกหมายเหตุ ติ๊กยืนยันว่าแจ้งสมาชิกแล้ว (PDPA) | - |
| จำลองข้อความ | `/simulator` | ใส่บทสนทนาทดสอบโดยไม่ต้องมี LINE | แผนสำรอง |
| Log การส่ง | `/logs` | ทุกข้อความที่ระบบส่งออก รวม dry-run และ mock พร้อม error | ตรวจสอบย้อนหลัง |

#### ลำดับการสาธิตที่แนะนำ (5 นาที)

1. `/summaries` เลือกกลุ่ม เลือกวันนี้ กด **สรุปด้วย AI**
2. ดูข้อความสรุปที่ได้ แล้วไปดู `/tasks` จะเห็นว่างานถูกสกัดเข้ามาให้แล้ว
3. กลับมา `/summaries` กด **ทดสอบ dry-run** แล้วดูใน `/logs`
4. กด **ส่งสรุปเข้ากลุ่ม** จริง แล้วดูในมือถือ
5. `/tasks` กดปิดงานที่ทำเสร็จแล้ว 1 รายการ แล้วกด **ส่งเตือน**

#### สองจุดออกแบบที่ต้องอธิบายให้ได้

**1. โหมด MOCK และ dry-run**

| โหมด | ตั้งค่าอย่างไร | ทำอะไร | ใช้เมื่อไหร่ |
| --- | --- | --- | --- |
| **MOCK** | `MOCK_LINE=true` ใน `.env` | ระบบทำงานครบทุกอย่างแต่ **ไม่ยิง LINE API** | ทดสอบ dashboard โดยยังไม่มี OA |
| **dry-run** | กดปุ่มในหน้าเว็บ หรือ `--dry-run` ใน CLI | ประกอบข้อความจริง บันทึก log จริง แต่ไม่ส่งออก | ตรวจตัวเลขก่อนส่งจริง |
| **ส่งจริง** | `MOCK_LINE=false` + มี token | ยิงเข้า LINE จริง | ใช้งานจริง |

> ✅ **หลักการที่ต้องจำ:** ระบบ automation ที่ดีต้องมีทั้ง MOCK และ dry-run มิฉะนั้นการทดสอบจะรบกวนคนในกลุ่มจริงและเปลืองโควต้า

**2. AI มี fallback**

ไฟล์ `src/services/summary.ts` ทำงาน 2 แบบ

```
มี ANTHROPIC_API_KEY ใน .env ?
        │
   ┌────┴────┐
  มี        ไม่มี
   │          │
   ▼          ▼
เรียก Claude API      ใช้ตัวสรุป rule-based ในเครื่อง
สรุป + สกัดงาน        (นับสถิติ + จับ pattern คำมอบหมายภาษาไทย)
คุณภาพดีกว่ามาก        คุณภาพพอใช้ แต่ระบบไม่ล้ม
```

> 🎓 **จุดสอน:** ระบบที่ดีต้อง **ไม่ล้มทั้งระบบเพราะขาด API key เดียว** ให้ degrade แต่ยังทำงานได้ (graceful degradation) นี่คือหลักการที่ใช้ได้กับทุกระบบที่พึ่งพาบริการภายนอก

---

### 🧩 โจทย์ท้าทาย: ให้ AI ต่อยอด Dashboard เอง

```
ในโปรเจกต์นี้มี Admin Dashboard เขียนด้วย Express + EJS อยู่แล้ว
ช่วยเพิ่มหน้าใหม่ /keywords ที่แสดง

1. คำที่ถูกพูดถึงบ่อยที่สุด 20 คำในกลุ่มที่เลือก (ตัดคำช่วยภาษาไทยออก)
2. กราฟแสดงจำนวนครั้งที่พบคำ 10 อันดับแรก ด้วย Chart.js
3. ช่องค้นหาที่กดคำแล้วเด้งไปหน้า /messages พร้อม filter คำนั้น

ให้เขียนตามสไตล์เดิมของโปรเจกต์
- service อยู่ใน src/services/ route อยู่ใน src/routes/admin.ts view อยู่ใน src/views/
- ใช้ asyncRouter เหมือน route อื่น
- ไม่ใส่ semicolon comment ภาษาไทย
- เพิ่มลิงก์ในเมนู src/views/partials/nav.ejs ด้วย
รันแล้วต้องผ่าน npm run typecheck
```

> 💡 **จุดสอนของ prompt นี้:** สังเกตว่าเราบอก **"ให้เขียนตามสไตล์เดิมของโปรเจกต์"** พร้อมระบุตำแหน่งไฟล์ให้ครบ นี่คือวิธีทำให้ AI เขียนโค้ดที่กลืนกับ codebase เดิม ไม่ใช่โค้ดแปลกปลอมที่ต้องมาแก้ทีหลัง และการปิดท้ายด้วยเงื่อนไขที่ตรวจได้ (`npm run typecheck` ต้องผ่าน) ทำให้ AI มีเกณฑ์วัดว่างานเสร็จหรือยัง

### ✅ Checkpoint Workshop 4

- [ ] `/mcp` เห็น `postgres-linechat` connected
- [ ] AI ตอบคำถาม B1-B4 ได้จากข้อมูลจริงในฐานข้อมูล
- [ ] สรุปถูกส่งกลับเข้ากลุ่มไลน์แล้ว (สมาชิกทุกคนเห็น)
- [ ] เปิด Dashboard ได้และกดสรุปจากหน้าเว็บได้
- [ ] เข้าใจความต่างของ MOCK / dry-run / ส่งจริง
- [ ] รู้ว่าทำไมควรใช้ read-only user กับ MCP ที่ต่อฐานข้อมูลจริง

---

## 🏆 Workshop 5 (Capstone): Executive Daily Report - รายงานธุรกิจอัตโนมัติ

### เวลา 22:15 - 23:00 น. (45 นาที)

> **บทบาทที่สวม:** ทีม IT ที่ทำระบบรายงานให้ผู้บริหารสยามสมาร์ทเทรด
> **เป้าหมาย:** เปลี่ยนจาก **"สั่ง AI ทีละครั้ง"** เป็น **"ระบบทำงานแทนเรา"**
> จบ Workshop นี้ ทุกกลุ่มจะมีระบบที่รันเองทุกเช้า 08:00 โดยไม่มีคนสั่ง

---

### Part A: Import ฐานข้อมูลยอดขายจำลอง (5 นาที)

```bash
npm run seed:sales
```

ได้ข้อมูล **30 วันย้อนหลังจากวันนี้ ประมาณ 390 บิล** ในตาราง `sales_orders`

```sql
sales_orders (
  order_no, order_date, branch, channel, sales_person,
  customer_name, product, qty, unit_price, amount, status
)
```

| มิติ | ค่าที่มีในข้อมูล |
| --- | --- |
| สาขา (5) | สำนักงานใหญ่, เชียงใหม่, ขอนแก่น, หาดใหญ่, ชลบุรี |
| ช่องทาง (5) | หน้าร้าน, LINE OA, Marketplace, ตัวแทนขาย, เว็บไซต์ |
| สินค้า | 8 รายการเครื่องใช้ไฟฟ้า |
| สถานะ | paid / pending / cancelled |

> 💡 **จุดออกแบบที่น่าสนใจ:** วันที่ในข้อมูลขยับตามวันปัจจุบันเสมอ ข้อมูลจึงสดใหม่ทุกครั้งที่ import และชุดนี้ใช้ `md5(เลขที่เอกสาร)` เป็นตัวสุ่มแทน `random()` เพราะ `random()` ใน SQL ที่มี LATERAL อาจถูกประเมินครั้งเดียวต่อกลุ่มแถว ทำให้ข้อมูลจำลองกระจุกตัว (ทุกบิลในวันเดียวกันได้สาขาเดียวกัน)

> ⚠️ **มีรายการผิดปกติซ่อนไว้ 4 แบบใน "เมื่อวาน"** อย่าเพิ่งดูเฉลย ให้ AI หาเองก่อนใน Part B

---

### Part B: Warm-up สั่ง AI วิเคราะห์ผ่าน MCP (10 นาที)

#### B1. ยอดขายเมื่อวาน

```
จากตาราง sales_orders ช่วยสรุปยอดขายของเมื่อวาน
- ยอดขายรวม จำนวนบิล ยอดเฉลี่ยต่อบิล
- แยกตามสาขา เรียงจากมากไปน้อย
- แยกตามช่องทางขาย
- สินค้าขายดี 5 อันดับ
- เทียบกับวันก่อนหน้าเป็นเปอร์เซ็นต์
ไม่นับรายการที่ status = 'cancelled'
แสดงตัวเลขแบบมี comma และหน่วยบาท
```

#### B2. Anomaly Detection (ไฮไลท์ของช่วงนี้)

```
ช่วยตรวจหารายการที่น่าสงสัยหรือผิดปกติในยอดขายเมื่อวาน
คิดเกณฑ์ตรวจสอบเองอย่างน้อย 4 แบบ เช่น
- ยอดต่อบิลสูงผิดปกติเมื่อเทียบกับค่าปกติ
- ราคาต่อหน่วยที่ต่างจากราคาปกติของสินค้าเดียวกันมาก
- ยอดติดลบหรือค่าที่เป็นไปไม่ได้
- รายการที่ดูเหมือนคีย์ซ้ำ
อธิบายเกณฑ์ที่ใช้ แสดง SQL และบอกว่าแต่ละรายการน่าจะเกิดจากอะไร
พร้อมเสนอว่าควรแก้ไขอย่างไรในทางบัญชี
```

**เฉลย: รายการที่ฝังไว้ 4 แบบ**

| เลขที่ | ปัญหา | ควรตรวจด้วยเกณฑ์อะไร |
| --- | --- | --- |
| SO-ANOM-001 | ขายแอร์ 40 เครื่องบิลเดียว 596,000 บาท | ยอดต่อบิล > เกณฑ์ (เช่น 300,000) |
| SO-ANOM-002 | หม้อทอด ราคาต่อหน่วย 259,000 (ปกติ 2,590) | ราคาต่อหน่วยเทียบ median ของสินค้าเดียวกัน > 3 เท่า |
| SO-ANOM-003 | ยอดติดลบ -12,500 (บันทึกคืนสินค้าผิดวิธี) | `amount < 0` ควรออกใบลดหนี้ ไม่ใช่ยอดขายติดลบ |
| SO-ANOM-004/005 | รายการเดียวกันคีย์ซ้ำ 2 ครั้ง | GROUP BY สาขา/ช่องทาง/พนักงาน/ลูกค้า/สินค้า/จำนวน/ยอด HAVING COUNT(*) > 1 |

**SQL จริงที่ชุด workshop ใช้ตรวจเกณฑ์ที่ 2 (ราคาต่อหน่วยผิดปกติ)** จากไฟล์ `src/services/sales.ts`

```sql
WITH med AS (
    SELECT product,
           percentile_cont(0.5) WITHIN GROUP (ORDER BY unit_price) AS median_price
      FROM sales_orders
     WHERE unit_price > 0
     GROUP BY product
)
SELECT s.order_no, s.product, s.unit_price, m.median_price
  FROM sales_orders s
  JOIN med m ON m.product = s.product
 WHERE s.order_date = $1::date
   AND s.unit_price > m.median_price * 3
```

> 💡 **ทำไมใช้ median ไม่ใช่ average:** ค่าเฉลี่ยถูกดึงโดยค่าผิดปกติได้ง่าย ถ้ามีรายการที่คีย์ราคาผิด 259,000 บาทอยู่ในชุดข้อมูล ค่าเฉลี่ยจะสูงขึ้นจนตรวจไม่เจอตัวเอง ส่วน **median (ค่ากลาง) ทนต่อค่าผิดปกติ** จึงเหมาะกับงาน anomaly detection

**SQL ตรวจรายการซ้ำ**

```sql
SELECT string_agg(order_no, ', ' ORDER BY order_no) AS order_nos,
       product,
       amount::text AS amount
  FROM sales_orders
 WHERE order_date = $1::date
 GROUP BY branch, channel, sales_person, customer_name, product, qty, amount
HAVING COUNT(*) > 1
```

> ⚠️ **จุดสอนสำคัญเรื่อง False Positive:** เกณฑ์ที่ AI คิดขึ้นเองอาจ **จับผิดเกินจริง** เช่น ถ้าเกณฑ์รายการซ้ำหลวมเกินไป (ดูแค่สินค้า + ยอด) จะจับข้อมูลปกติที่บังเอิญเหมือนกันมาด้วย สังเกตว่า SQL จริงข้างบน `GROUP BY` ถึง 7 คอลัมน์ เพื่อให้แน่ใจว่าเป็นรายการเดียวกันจริง ๆ
>
> **ให้ผู้เรียนลองปรับเกณฑ์และดูว่าจำนวนที่ตรวจพบเปลี่ยนไปอย่างไร นี่คือหัวใจของงาน data quality**

#### B3. หา insight เชิงธุรกิจ

```
จากข้อมูล 30 วันในตาราง sales_orders ช่วยหา insight 5 ข้อที่ผู้บริหารควรรู้
เช่น สาขาที่โตเร็ว/ถดถอย ช่องทางที่ทำกำไรดี วันในสัปดาห์ที่ขายดี
พร้อมตัวเลขสนับสนุนทุกข้อ และแยกให้ชัดว่าข้อไหนเป็น "ข้อเท็จจริงจากข้อมูล"
กับข้อไหนเป็น "ข้อสันนิษฐาน" ของคุณ
```

> 🎓 **จุดสอน:** การบังคับให้ AI **แยกข้อเท็จจริงออกจากข้อสันนิษฐาน** เป็นเทคนิคที่ควรใช้กับทุกงานวิเคราะห์ เพราะรายงานที่เอาไปเสนอผู้บริหารต้องแยกได้ว่าอะไรคือตัวเลขจริง อะไรคือการตีความ

---

### Part C: อ่านสคริปต์ daily-report จากชุดไฟล์ (20 นาที)

#### C1. เปิดสคริปต์จริงมาไล่ทีละขั้น

เปิด 2 ไฟล์นี้คู่กัน

| ไฟล์ | หน้าที่ |
| --- | --- |
| `scripts/daily-report.ts` | ทางเข้าของสคริปต์ อ่าน argument แล้วเรียกตรรกะ |
| `src/jobs/dailyReport.ts` | ตรรกะจริง query + ตรวจ anomaly + ประกอบ Flex + ส่ง |

**สิ่งที่สคริปต์ทำตามลำดับ ให้ไล่หาให้ครบทั้ง 7 ข้อ**

1. รับ argument จาก command line
   - `--date=YYYY-MM-DD` วันที่ของรายงาน (ค่าเริ่มต้น = เมื่อวาน)
   - `--group=Cxxxx` groupId ปลายทาง (ถ้าไม่ใส่จะอ่านจาก `DEFAULT_GROUP_ID` ใน `.env`)
   - `--dry-run` ประกอบข้อความและ log แต่ไม่ส่งออกจริง
2. query จากตาราง `sales_orders` หายอดรวม จำนวนบิล ยอดเฉลี่ยต่อบิล (ไม่นับ status `cancelled`) เทียบกับวันก่อนหน้าเป็นเปอร์เซ็นต์ และแยกตามสาขาและช่องทาง
3. ตรวจรายการผิดปกติ 4 เกณฑ์ ยอดต่อบิลเกิน 300,000, ราคาต่อหน่วยเกิน 3 เท่าของ median ของสินค้าเดียวกัน, ยอดติดลบ, รายการซ้ำ
4. ประกอบ Flex Message bubble ขนาด mega หัวสีน้ำเงินเข้ม `#0C1628` ยอดรวมตัวใหญ่พร้อมลูกศรขึ้น/ลง ตารางสาขาและช่องทาง และกล่องแดงอ่อนเมื่อพบรายการผิดปกติ (เขียวอ่อนเมื่อไม่พบ)
5. ส่งเข้ากลุ่มด้วย **Push API ของ `@line/bot-sdk` ไม่ใช่ MCP**
6. พิมพ์สรุปลง console ทุกครั้งเพื่อให้ตรวจ log ย้อนหลังได้
7. ปิด connection pool และ exit code 0 เมื่อสำเร็จ / 1 เมื่อ error

**3 จุดออกแบบที่ควรชี้ให้ดูในโค้ด**

- ค่าเกณฑ์ anomaly ทุกตัวอ่านจาก `.env` ไม่ hard-code ทำให้แต่ละบริษัทปรับเกณฑ์เองได้โดยไม่ต้องแก้โค้ด
- กรณีไม่พบข้อมูลของวันที่ระบุ จะคืนสถานะ `no-data` พร้อมบอกวันที่ล่าสุดที่มีข้อมูล แทนที่จะ crash
- `--dry-run` ทำงานครบทุกขั้นยกเว้นการยิง API จริง จึงทดสอบได้โดยไม่กินโควต้าข้อความ

> 🎓 **จุดสอนข้อ 5 ที่สำคัญมาก:** ทำไมสคริปต์ต้องใช้ **Push API ตรง ๆ ไม่ใช่ MCP**
>
> เพราะ MCP ต้องมี **AI Agent เป็นคนเรียก** แต่สคริปต์ที่รันตอนตี 8 โดย Task Scheduler ไม่มี AI นั่งอยู่ตรงนั้น งานประจำที่รูปแบบตายตัวจึงควรเป็นโค้ดธรรมดา เสถียรกว่า เร็วกว่า และไม่กินค่า token
>
> **AI Agent คือคนเขียนสคริปต์ให้ ไม่ใช่คนรันสคริปต์แทนทุกวัน**

> 💡 **ให้ AI ช่วยอ่านโค้ดก็ได้:** เปิด `claude` ในโฟลเดอร์โปรเจกต์แล้วสั่งว่า *"อธิบาย `src/jobs/dailyReport.ts` ให้ฟังทีละขั้น โดยเฉพาะเกณฑ์ตรวจ anomaly ทั้ง 4 ข้อว่าคำนวณจากอะไร และมีเคสไหนที่จะ false positive"* นี่คือการใช้ AI แบบที่ได้ประโยชน์ที่สุดในงานจริง คือใช้ทำความเข้าใจโค้ดที่มีอยู่แล้ว ไม่ใช่ให้เขียนใหม่ทุกครั้ง

#### C2. ทดสอบ (สำคัญ ห้ามข้าม)

```bash
# 1) ทดสอบแบบไม่ส่งออก ดูตัวเลขก่อน
npm run daily-report -- --dry-run

# 2) เขียนรายงาน HTML เก็บไว้ดูด้วย
npm run daily-report -- --dry-run --html

# 3) ระบุวันที่ที่มี anomaly ชัด ๆ (เมื่อวาน)
npm run daily-report -- --dry-run --date=2026-08-08

# 4) พอใจแล้วจึงส่งจริง
npm run daily-report -- --group=<groupId ของกลุ่มผู้บริหาร>
```

**ตัวอย่างผลลัพธ์ที่ควรได้**

```
[daily-report] รายงานวันที่ 2026-07-24 (โหมด dry-run)
  ---------------------------------------------
  ยอดขายรวม      : 1,234,130 บาท
  จำนวนบิล        : 16
  ยอดเฉลี่ย/บิล   : 77,133 บาท
  เทียบวันก่อน    : +211.4%
  สาขา:
    - สาขาหาดใหญ่: 613,970 บาท (2 บิล)
    ...
  [!] พบรายการผิดปกติ 4 รายการ
    - ยอดสูงผิดปกติ (SO-ANOM-001): สาขาหาดใหญ่ 596,000 บาท สูงกว่าเกณฑ์ 300,000 บาท
    - ราคาต่อหน่วยผิดปกติ (SO-ANOM-002): หม้อทอดไร้น้ำมัน คีย์ราคา 259,000 บาท ปกติราว 2,590 บาท
    - ยอดติดลบ (SO-ANOM-003): ตู้เย็น 2 ประตู 9 คิว -12,500 บาท ควรออกเป็นใบลดหนี้
    - รายการซ้ำ: เครื่องกรองน้ำ RO 26,700 บาท ปรากฏซ้ำ (SO-ANOM-004, SO-ANOM-005)
  ---------------------------------------------
```

> 💡 **ลองสั่ง `npm run daily-report -- --dry-run --date=2026-01-01`** (วันที่ที่ไม่มีข้อมูลแน่ ๆ) เพื่อดูว่าสคริปต์ตอบสถานะ `no-data` พร้อมบอกวันที่ล่าสุดที่มีข้อมูลจริงหรือไม่ นี่คือการทดสอบ edge case ที่โปรแกรมเมอร์มือใหม่มักลืม

---

### Part D: ตั้งเวลารันอัตโนมัติ (10 นาที)

#### D1. เตรียมไฟล์ .bat (Windows)

สร้าง `run-daily-report.bat` ไว้ที่โฟลเดอร์รากของโปรเจกต์

```bat
@echo off
cd /d "C:\line-workflow-app"
call npm run daily-report -- --group=Cxxxxxxxxxxxxxxxxxxxx >> logs\daily-report.log 2>&1
```

สร้างโฟลเดอร์ `logs` ก่อน แล้วทดสอบดับเบิลคลิกไฟล์ .bat ดูว่าทำงานได้

> 💡 **ให้ AI ช่วยก็ได้:** *"ช่วยสร้างไฟล์ .bat สำหรับ Windows Task Scheduler ที่เข้าโฟลเดอร์โปรเจกต์ แล้วรัน npm run daily-report โดยเก็บ log ต่อท้ายไฟล์ logs\daily-report.log พร้อมประทับวันเวลา"*

#### D2. ตั้ง Windows Task Scheduler

1. เปิด **Task Scheduler** > **Create Task** (ไม่ใช่ Basic Task)
2. tab **General**
   - Name: `LINE Daily Sales Report`
   - เลือก **Run whether user is logged on or not**
   - ติ๊ก **Run with highest privileges**
3. tab **Triggers** > New
   - Begin the task: On a schedule
   - Daily เวลา **08:00:00** ติ๊ก Enabled
4. tab **Actions** > New
   - Action: Start a program
   - Program/script: `C:\line-workflow-app\run-daily-report.bat`
   - Start in: `C:\line-workflow-app`
5. tab **Conditions** > ปลดติ๊ก **Start the task only if the computer is on AC power** (สำหรับ notebook)
6. tab **Settings** > ติ๊ก **Run task as soon as possible after a scheduled start is missed**
7. บันทึก แล้วคลิกขวาที่ task > **Run** เพื่อทดสอบทันที

> ⚠️ **ข้อ 4 ช่อง "Start in" คือจุดที่คนลืมบ่อยที่สุด** ถ้าไม่ใส่ Task Scheduler จะรันในโฟลเดอร์ระบบ หา `node_modules` ไม่เจอ แล้วขึ้น Last Run Result เป็น `0x1`

**ตรวจว่าสำเร็จ 3 จุด**

| ตรวจอะไร | ผลที่ต้องได้ |
| --- | --- |
| ไฟล์ `logs\daily-report.log` | มีข้อความรายงานเขียนต่อท้าย |
| Last Run Result ใน Task Scheduler | `0x0` (0x1 = ผิดพลาด) |
| ไลน์กลุ่มผู้บริหาร | มีข้อความ Flex รายงานเข้ามา |

#### D3. ทางเลือกอื่นของการตั้งเวลา

| วิธี | เหมาะกับ | วิธีตั้ง |
| --- | --- | --- |
| **Windows Task Scheduler** | เครื่องผู้ใช้ / เซิร์ฟเวอร์ Windows (ใช้ในคลาส) | ตามข้างบน |
| **ตัวตั้งเวลาในตัวโปรแกรม** | deploy บน cloud ที่รันค้างตลอด เช่น Render Starter | ตั้ง `ENABLE_SCHEDULER=true` |
| cron (macOS/Linux) | เซิร์ฟเวอร์ Linux ที่รันเอง | `0 8 * * * cd /path/to/line-workflow-app && npm run daily-report` |
| GitHub Actions | ไม่มีเซิร์ฟเวอร์เลย | `schedule: - cron: "0 1 * * *"` (UTC) |
| **Claude Cowork / Claude Code Routines** | งานที่พึ่ง connector ล้วน ไม่แตะไฟล์หรือฐานข้อมูลในเครื่อง | สร้างจากหน้า **Scheduled** / **Routines** ในแอป หรือสั่ง Claude ด้วยภาษาพูด |

**ตัวตั้งเวลาในตัวโปรแกรม** เตรียมไว้ให้แล้วที่ `src/scheduler.ts` เปิดใช้ด้วย 3 ค่านี้ใน `.env`

```env
ENABLE_SCHEDULER=true
SUMMARY_CRON=0 18 * * *     # สรุปแชทกลุ่มทุกเย็น 18:00
REPORT_CRON=0 8 * * *       # รายงานผู้บริหารทุกเช้า 08:00
```

เวลาอ้างตามค่า `TZ` (ค่าเริ่มต้น `Asia/Bangkok`) จึงใส่เวลาไทยได้ตรง ๆ ไม่ต้องแปลง UTC

```typescript
cron.schedule(
  config.scheduler.reportCron,
  async () => {
    const outcome = await runDailyReport({ sentBy: 'scheduler:daily-report' })
    if (outcome.sendStatus === 'no-data') {
      console.log(`[scheduler]   ไม่พบข้อมูลขายของวันที่ ${outcome.date}`)
      return
    }
    printReport(outcome)
  },
  { timezone: tz }        // สำคัญ: ระบุ timezone ให้ node-cron เสมอ
)
```

> ⚠️ **คำเตือนเรื่อง timezone:** cron ของ GitHub Actions ใช้ **UTC** ต้องลบ 7 ชั่วโมงจากเวลาไทย (08:00 ไทย = 01:00 UTC) แต่ตัวตั้งเวลาในตัวโปรแกรมและ Task Scheduler ใช้เวลาท้องถิ่นตรง ๆ

> 🎓 **จุดสอนสถาปัตยกรรมที่สำคัญที่สุดของ Capstone:** ตรรกะของงานอยู่ที่ `src/jobs/dailyReport.ts` **ที่เดียว** แต่ถูกเรียกได้ 3 ทาง
>
> ```
>                     src/jobs/dailyReport.ts   (ตรรกะอยู่ที่นี่ที่เดียว)
>                               ▲
>           ┌───────────────────┼───────────────────┐
>           │                   │                   │
>    scripts/daily-report.ts  src/scheduler.ts   routes/admin.ts
>    (CLI + Task Scheduler)   (cron ในโปรเซส)     (ปุ่มในหน้า /sales)
> ```
>
> การแยกตรรกะออกจาก "ทางเข้า" ทำให้ย้ายจาก Task Scheduler ไป cloud ได้ **โดยไม่ต้องเขียนโค้ดใหม่เลย** เปลี่ยนแค่ว่าใครเป็นคนเรียก หลักการนี้คือ `services/` และ `jobs/` **ไม่ import อะไรจาก express เลย**

---

#### D4. ตั้งเวลาแบบไหนเลือกอย่างไร

> 💡 **หัวใจของหัวข้อนี้:** ตัวตั้งเวลามีให้เลือกหลายแบบ แต่คำถามที่ตัดสินได้เร็วที่สุดมีแค่ **2 ข้อ** คือ (1) งานนี้ต้องแตะไฟล์หรือฐานข้อมูลในเครื่องหรือไม่ และ (2) เครื่องเราเปิดอยู่ตลอดหรือไม่ ตอบ 2 ข้อนี้ได้ ก็เลือกได้ทันที

**แผนผังตัดสินใจ**

```
                     งานนี้ต้องแตะไฟล์ / ฐานข้อมูล / โปรแกรมในเครื่องไหม
                                        │
              ┌─────────────────────────┴─────────────────────────┐
             ใช่                                                 ไม่ใช่
              │                                          (ใช้แค่ connector เช่น
              ▼                                           Gmail, Calendar, Slack,
      เครื่องเปิดตลอดไหม                                   Drive, ค้นเว็บ)
              │                                                   │
      ┌───────┴────────┐                                          ▼
     เปิด           ไม่เปิด                              Claude Cowork /
      │                 │                               Claude Code Routines
      ▼                 ▼                                (รันบนคลาวด์ ปิดเครื่องได้)
Windows Task      ย้ายขึ้น cloud ก่อน
Scheduler /       (Render Starter + Neon)
cron ในเครื่อง     แล้วใช้ ENABLE_SCHEDULER=true
                  หรือ GitHub Actions
```

**ตารางเทียบทั้ง 5 ทางเลือก**

| ตัวตั้งเวลา | รันที่ไหน | ต้องเปิดเครื่องไหม | เข้าถึงไฟล์/DB ในเครื่อง | ถี่ที่สุด | ค่าใช้จ่าย | เหมาะกับงานแบบไหน |
| --- | --- | --- | --- | --- | --- | --- |
| **Windows Task Scheduler** | เครื่องเรา | **ต้อง** (และต้องไม่หลับ) | ได้เต็มที่ | 1 นาที | ฟรี | ใช้ในคลาส งานที่ต่อ PostgreSQL ในเครื่อง |
| **cron (macOS/Linux)** | เซิร์ฟเวอร์เรา | ต้อง (เซิร์ฟเวอร์เปิดตลอดอยู่แล้ว) | ได้เต็มที่ | 1 นาที | ค่าเซิร์ฟเวอร์ | เซิร์ฟเวอร์ Linux ในองค์กร |
| **ตัวตั้งเวลาในตัวโปรแกรม** (node-cron) | โปรเซสเดียวกับเว็บ | ไม่ต้อง (ถ้า deploy แล้ว) | ได้ (ของเซิร์ฟเวอร์นั้น) | 1 นาที | รวมในค่า hosting | **แนะนำสำหรับระบบนี้** เมื่อขึ้น Render Starter |
| **GitHub Actions** | คลาวด์ของ GitHub | ไม่ต้อง | ไม่ได้ (ต้องต่อ DB ผ่านเน็ต) | 5 นาที (จริงมักช้ากว่า) | ฟรีในโควต้า | งานที่ไม่มีเซิร์ฟเวอร์เลย |
| **Claude Cowork / Claude Code Routines** | คลาวด์ของ Anthropic | **ไม่ต้อง** | **ไม่ได้** | 1 ชั่วโมง | รวมในแพลน Pro ขึ้นไป | งานสรุป/รายงานที่ดึงจาก connector ล้วน |

---

##### Claude Cloud Routines เหมาะกับอะไร และไม่เหมาะกับอะไร

ปี 2026 Claude Cowork และ Claude Code Desktop เพิ่มความสามารถตั้งเวลางานให้รันบน **คลาวด์ของ Anthropic** ได้ โดยเอกสารทางการระบุว่างานจะรันตามรอบเวลา **แม้คอมพิวเตอร์จะหลับหรือปิดแอปไปแล้ว** ใช้ได้กับทุกแพลนแบบเสียเงิน (Pro, Max, Team, Enterprise)

| ✅ เหมาะกับ | ❌ ไม่เหมาะกับ |
| --- | --- |
| สรุปอีเมลและปฏิทินประจำเช้า | รันสคริปต์ที่ต่อ PostgreSQL ในเครื่อง |
| สรุปข้อความ Slack / Teams รายวัน | อ่านหรือเขียนไฟล์ในโฟลเดอร์บนเครื่อง |
| ติดตามข่าวคู่แข่งรายสัปดาห์ด้วยการค้นเว็บ | งานที่ต้องรันคำสั่ง `npm run` ในโปรเจกต์ของเรา |
| รวบรวมข้อมูลจาก Google Drive เป็นรายงาน | งานที่ต้องการความถี่ต่ำกว่า 1 ชั่วโมง |
| งานที่ทำผ่าน connector และไฟล์ในบัญชี Claude เท่านั้น | งานที่ต้องเข้าถึงระบบหลัง firewall ขององค์กร |

> ⚠️ **ข้อจำกัดที่ต้องรู้ก่อนวางแผน (สำคัญที่สุด):** เอกสารของ Anthropic ระบุชัดว่า scheduled tasks ทำงานกับ **connectors และไฟล์ที่เก็บอยู่ในบัญชี Claude เท่านั้น ผูกกับโฟลเดอร์บนเครื่องไม่ได้** และในหน้าสร้าง task แบบ manual มีคำเตือนว่า *"If a scheduled task requires local files or apps, it will only run locally"*
>
> แปลว่า **ทันทีที่งานแตะไฟล์ในเครื่อง มันจะตกกลับมารันบนเครื่องเรา** ถ้าตอนนั้นเครื่องหลับอยู่ งานก็จะไม่รันเลย ไม่ใช่ว่ารันบนคลาวด์แทนให้

**เทียบ 3 โหมดของฝั่ง Claude Code Desktop**

| | Cloud (Routines) | Desktop (local task) | `/loop` ใน CLI |
| --- | --- | --- | --- |
| รันที่ไหน | คลาวด์ของ Anthropic | เครื่องเรา | เครื่องเรา |
| ต้องเปิดเครื่อง | ไม่ต้อง | ต้อง | ต้อง |
| ต้องเปิด session ค้างไว้ | ไม่ต้อง | ไม่ต้อง | **ต้อง** |
| เข้าถึงไฟล์ในเครื่อง | ไม่ได้ | ได้ | ได้ |
| ช่วงถี่ที่สุด | 1 ชั่วโมง | 1 นาที | 1 นาที |

> 🎓 **จุดสอนสำหรับหลักสูตรนี้โดยเฉพาะ:** Capstone ของเรารัน `npm run daily-report` ที่ต่อ PostgreSQL ในเครื่อง จึงเป็นงานที่ **แตะทั้งไฟล์และโปรเซสในเครื่องเต็มตัว** Claude Cloud Routines **ยังใช้แทน Windows Task Scheduler ในเคสนี้ไม่ได้**
>
> ถ้าต้องการให้รายงานเช้าทำงานโดยไม่ต้องเปิดคอมพิวเตอร์ทิ้งไว้ คำตอบที่ถูกต้องยังเป็นเส้นทางเดิมของหลักสูตร คือ **ย้ายระบบขึ้น Render Starter + Neon แล้วเปิด `ENABLE_SCHEDULER=true`** ไม่ใช่ย้ายไปใช้ตัวตั้งเวลาของ Claude

**แล้วเมื่อไหร่ที่ Claude Cloud Routines มีประโยชน์กับงานสาย LINE**

ถ้าออกแบบใหม่ให้ **ไม่แตะฐานข้อมูลในเครื่อง** ก็ใช้ได้ เช่น

- สรุปอีเมลและปฏิทินตอนเช้าแล้วให้ Claude ส่งเข้า LINE ผ่าน LINE Bot MCP (ทุกอย่างเป็น connector ล้วน)
- ติดตามข่าวอุตสาหกรรมรายสัปดาห์แล้วสรุปส่งเข้ากลุ่มผู้บริหาร
- ดึงข้อมูลจาก Google Drive มาสรุปแล้วแจ้งเตือนทีม

> 💡 **หลักคิดที่ใช้ตัดสินใจได้เสมอ:** **งานที่ข้อมูลอยู่บนคลาวด์อยู่แล้ว ให้ตั้งเวลาบนคลาวด์ ส่วนงานที่ข้อมูลอยู่ในองค์กร ให้ตั้งเวลาไว้ใกล้ข้อมูล** อย่าฝืนให้ตัวตั้งเวลาที่อยู่คนละฝั่งกับข้อมูลเป็นคนทำงาน เพราะจะได้ระบบที่เปราะและ debug ยาก

**เอกสารอ้างอิงของหัวข้อนี้**

- Schedule recurring tasks in Claude Cowork: https://support.claude.com/en/articles/13854387-schedule-recurring-tasks-in-claude-cowork
- Schedule recurring tasks in Claude Code Desktop: https://code.claude.com/docs/en/desktop-scheduled-tasks

---

### 🧩 Part E: โจทย์ท้าทายต่อยอด (เลือกทำ)

1. **แจ้งเตือนเฉพาะเหตุผิดปกติ (alert-only pattern)** - ทำไว้ให้แล้วเป็นตัวอย่าง ลองรัน
   ```bash
   npm run daily-report -- --alert-only --dry-run
   ```
   ดูว่าเมื่อไม่พบ anomaly ระบบจะไม่ส่งเข้ากลุ่ม แล้วอ่านโค้ดใน `src/jobs/dailyReport.ts` ว่าทำอย่างไร
   *(หลักคิด: ลดเสียงรบกวนผู้บริหาร รายงานที่ส่งทุกวันจนคนเลิกอ่าน มีค่าเท่ากับไม่ส่ง)*
2. **รายงานสรุปสัปดาห์** - สคริปต์ใหม่ที่รันเช้าวันจันทร์ สรุป 7 วันพร้อมกราฟ HTML แนบลิงก์
3. **เตือนเป้ายอดขาย** - ถ้ายอดสะสมเดือนต่ำกว่าเป้าเกิน 10% ให้ส่งเตือนแยกอีกข้อความ
4. **รวมสองระบบเข้าด้วยกัน** - รายงานเช้าเพิ่มบรรทัด "งานค้างจากกลุ่มปฏิบัติการ N รายการ" ดึงจาก `group_tasks` (ใช้ข้อมูลทั้ง 2 Workshop ในรายงานเดียว)

### ✅ Checkpoint สุดท้ายของหลักสูตร

- [ ] `npm run daily-report -- --dry-run` แสดงตัวเลขถูกต้อง
- [ ] AI หา anomaly ได้ครบทั้ง 4 แบบ
- [ ] ส่ง Flex รายงานเข้ากลุ่มผู้บริหารได้จริง
- [ ] Windows Task Scheduler รันได้ Last Run Result = `0x0`
- [ ] **มีข้อความรายงานเข้ากลุ่มโดยที่เราไม่ได้สั่งอะไรเลย** ← นี่คือเป้าหมายของทั้งหลักสูตร
- [ ] อธิบายได้ว่างานแบบไหนควรสั่ง AI สด งานแบบไหนควรเป็นสคริปต์ตั้งเวลา
- [ ] เลือกตัวตั้งเวลาที่เหมาะกับงานได้ และอธิบายได้ว่าทำไม Claude Cloud Routines ใช้แทน Task Scheduler ในเคสนี้ไม่ได้

---

## 📚 Module 4: Codex Cross-check, Governance และแนวทางต่อยอด

### เวลา 23:00 - 23:30 น.

---

### 4.1 รัน workflow ชุดเดียวกันบน GPT Codex

ใช้ prompt B1, B3 และ B5 ของ Workshop 4 รันบน Codex CLI (ที่ตั้งค่า MCP ไว้ตั้งแต่เมื่อวาน) แล้วเพิ่ม Postgres MCP เข้าไปด้วย

```toml
# ~/.codex/config.toml  (Windows: %USERPROFILE%\.codex\config.toml)

[mcp_servers.postgres-linechat]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-postgres", "postgresql://postgres:รหัสผ่าน@localhost:5432/linechat"]
```

| หัวข้อเปรียบเทียบ | Claude Code | GPT Codex |
| --- | --- | --- |
| วิธีตั้งค่า MCP | `claude mcp add` | แก้ไฟล์ `config.toml` เอง |
| ตรวจสถานะ MCP | `/mcp` | ดูใน session ตอนเริ่ม |
| คุณภาพ SQL ที่เขียน | | |
| การขอยืนยันก่อนส่งข้อความ | | |
| ความเข้าใจภาษาไทยในบทสนทนา | | |

---

### 4.2 Governance และ Privacy ที่ต้องย้ำก่อนปิดคอร์ส

| ประเด็น | แนวปฏิบัติ |
| --- | --- |
| **ความยินยอม** | แจ้งสมาชิกกลุ่มทุกคนก่อนเก็บบทสนทนา บันทึกไว้ว่าแจ้งเมื่อไหร่ (ระบบมีคอลัมน์ `consent_at`) |
| **สิทธิ์ฐานข้อมูล** | MCP ที่ต่อฐานข้อมูลจริงให้ใช้ user แบบ **read-only** เท่านั้น |
| **ข้อมูลอ่อนไหว** | ห้ามให้ข้อมูลลูกค้า เลขบัตรประชาชน ข้อมูลสุขภาพ เข้าไปในรายงานหรือ prompt |
| **Token** | เก็บใน `.env` ห้าม commit ห้ามวางใน chat และเปลี่ยนทันทีเมื่อสงสัยว่ารั่ว |
| **ระยะเวลาเก็บข้อมูล** | กำหนดนโยบายลบข้อความเก่า เช่น เก็บ 90 วัน แล้วลบอัตโนมัติ |
| **ตรวจสอบย้อนหลัง** | ทุกการส่งออกต้องมี log (ตาราง `message_logs`) ว่าใครสั่งส่งอะไรเมื่อไหร่ |

> ⚠️ **ประโยคที่ต้องพูดกับทีมก่อนติดตั้งระบบนี้ในองค์กรจริง:** "ระบบนี้บันทึกทุกข้อความในกลุ่ม ใครไม่สะดวกบอกได้" การมีเทคโนโลยีที่ทำได้ ไม่ได้แปลว่าควรทำโดยไม่ถาม

---

### 4.3 สิ่งที่ต้องแก้ก่อนนำไปใช้ใน production จริง

ชุดนี้ออกแบบเพื่อ **การเรียนการสอน** ถ้าจะขึ้น production ต้องเพิ่มอย่างน้อย

| ประเด็น | สถานะในชุดนี้ | ต้องทำอะไรเพิ่ม |
| --- | --- | --- |
| Session | **พร้อม** เก็บใน PostgreSQL เมื่อ `NODE_ENV=production` | - |
| HTTPS / cookie | **พร้อม** `trust proxy` + `cookie.secure` เปิดอัตโนมัติบน production | ถ้ารันเองต้องมี Nginx/Caddy หน้า |
| SSL ของฐานข้อมูล | **พร้อม** เปิดอัตโนมัติเมื่อเจอ `sslmode=` ใน `DATABASE_URL` | - |
| ตัวตั้งเวลา | **พร้อม** `ENABLE_SCHEDULER=true` | ถ้า scale เกิน 1 instance ต้องย้ายไป cron ภายนอก |
| ระบบผู้ใช้ | บัญชีเดียวใน env | ตาราง users + role + audit log การล็อกอิน |
| Rate limiting | ไม่มี | `express-rate-limit` ที่หน้า login เป็นอย่างน้อย |
| CSRF | ไม่มี | csrf token ในฟอร์ม POST ทุกฟอร์ม |
| Backup | ไม่มี | backup ของ Neon หรือ `pg_dump` ตั้งเวลา + **ทดสอบ restore จริง** |
| Monitoring | log ลง console | ส่ง log ออกที่ระบบกลาง + alert เมื่อ webhook เงียบเกิน N ชั่วโมง |
| การทดสอบ | ทดสอบด้วยมือ | unit test ของ `services/` + integration test ของ webhook |

---

### 4.4 เส้นทาง deploy ที่หลักสูตรแนะนำ: Render Starter + Neon

| ส่วน | บริการ | ค่าใช้จ่าย |
| --- | --- | --- |
| เว็บ + webhook + dashboard | **Render** แผน Starter | ประมาณ 7 USD/เดือน |
| ฐานข้อมูล | **Neon** แผนฟรี | 0 บาท (ไม่มีวันหมดอายุ) |
| ตัวตั้งเวลา | ตัวตั้งเวลาในตัวโปรแกรม (`ENABLE_SCHEDULER=true`) | 0 บาท (ไม่ต้องซื้อ Cron Job แยก) |

> ⚠️ **ทำไมไม่ใช้แผนฟรีของ Render:** แผนฟรีจะ sleep หลังไม่มี traffic 15 นาที และตื่นช้า 30-60 วินาที แต่ LINE ต้องได้ HTTP 2xx กลับภายในราว 2 วินาที ไม่งั้นข้อความจะหลุด
>
> **ทำไมไม่ใช้ Postgres ฟรีของ Render:** แบบฟรีหมดอายุใน 30 วันแล้วข้อมูลถูกลบ ส่วน Neon ฟรีไม่มีวันหมดอายุ

รายละเอียดทีละขั้นอยู่ใน `docs/10-Deployment-Render-Neon.md` และมี `render.yaml` (Blueprint) เตรียมไว้ให้แล้ว

---

### 4.5 แนวทางต่อยอดหลังคอร์ส

| ทิศทาง | ทำอะไร | ระดับความยาก |
| --- | --- | --- |
| **สร้าง MCP Server ของตัวเอง** | ห่อ API ภายในองค์กร (ERP, HRIS) ให้ AI เรียกใช้ได้ | ★★★ |
| **Alert เฉพาะเหตุการณ์ผิดปกติ** | ขยาย alert-only pattern ไปกับ metric อื่น | ★ |
| **รายงาน HTML ประจำสัปดาห์** | รวมกราฟ Chart.js ส่งลิงก์เข้าไลน์ | ★★ |
| **ย้ายระบบขึ้น Cloud** | Render + Neon ตามคู่มือ | ★★ |
| **เชื่อม MCP ตัวอื่น** | GitHub, Google Drive, Slack ใช้หลักการเดียวกันทุกประการ | ★★ |
| **นโยบายลบข้อมูลอัตโนมัติ** | สคริปต์ลบข้อความเก่าเกิน 90 วัน ตั้งเวลารัน | ★ |
| **เก็บรูปและไฟล์จากกลุ่ม** | ดึง content จาก LINE มาเก็บเอง ทำหน้าแกลเลอรีใน Dashboard (ดู Workshop 6) | ★★ |

---

## 📦 Workshop 6 (โมดูลเสริม): เก็บรูปและไฟล์เอกสารจากกลุ่มไว้ที่เซิร์ฟเวอร์ของเรา

> **สถานะ:** โมดูลเสริม **ไม่อยู่ในกำหนดการหลัก** ใช้สอนเมื่อเวลาเหลือ หรือให้ผู้เรียนอ่านต่อเองหลังเลิกคลาส
> **เวลาที่ใช้ถ้าสอนจริง:** ประมาณ 20-25 นาที
> **ของที่ใช้:** ตาราง `media_files` และ view `v_media_gallery` ที่ `npm run db:setup` สร้างไว้ให้แล้วตั้งแต่ Workshop 3

---

### 6.1 ทำไมต้องเก็บไฟล์ไว้เอง

Workshop 3 เราเก็บได้แค่ **metadata** ของรูปและไฟล์ (รู้ว่าใครส่ง เมื่อไร ประเภทอะไร) แต่ตัวไฟล์จริงยังอยู่บนเซิร์ฟเวอร์ของ LINE ซึ่งมีปัญหา 2 ข้อ

```
ปัญหาข้อ 1: LINE ลบไฟล์ทิ้งเมื่อผ่านไประยะหนึ่ง
            เอกสารทางการระบุว่า "Content is automatically deleted after
            a certain period from when the message was sent.
            There is no guarantee for how long content is stored."
            คือ LINE ไม่รับประกันและไม่ประกาศระยะเวลาที่แน่นอน

ปัญหาข้อ 2: ไม่มีทางค้นย้อนหลังได้
            ใบเสร็จที่ทีมส่งเข้ากลุ่มเมื่อ 3 เดือนก่อน หาไม่เจอแล้ว
            ต้องเลื่อนหาในแชทมือถือทีละหน้า
```

> 🎓 **จุดสอน:** นี่คือหลักคิดเดียวกับ Module 3 ทั้งวัน คือ **"เมื่อ API ปลายทางไม่ให้สิ่งที่เราต้องการ ให้เปลี่ยนจากดึงเมื่อต้องใช้ เป็นรับไว้ตั้งแต่ต้น"** ข้อความเราทำไปแล้วใน Workshop 3 ส่วนไฟล์คือของที่เหลือ

---

### 6.2 กลไกการดึงไฟล์ (Get content)

**สิ่งที่ดึงได้และไม่ได้**

| ประเภท | ดึงไฟล์จริงได้ไหม | หมายเหตุ |
| --- | --- | --- |
| `image` | ✅ | ได้ทั้งภาพต้นฉบับและภาพย่อ (preview) |
| `video` | ✅ | ไฟล์ใหญ่อาจต้องรอ LINE เตรียมข้อมูลก่อน |
| `audio` | ✅ | เหมือน video |
| `file` | ✅ | PDF, Excel, Word ที่ผู้ใช้ส่งเข้ากลุ่ม |
| `sticker` | ❌ | ไม่ใช่ content ที่ผู้ใช้ส่ง เก็บได้แค่ `stickerId` / `packageId` |
| `text` | ❌ | **ไม่มี API ดึงข้อความย้อนหลัง** ต้องเก็บตอนรับ webhook เท่านั้น |
| `location` | ❌ | เก็บ latitude/longitude จาก webhook ได้เลย ไม่ต้องดึง |

> ⚠️ **เงื่อนไขสำคัญ:** ดึงได้เฉพาะเมื่อ `contentProvider.type` ใน webhook event เป็น `line` เท่านั้น ถ้าเป็น `external` แปลว่าไฟล์อยู่ URL ภายนอก LINE ไม่ได้เก็บให้ ต้องอ่านจาก `contentProvider.originalContentUrl` เอง

**Endpoint ที่ใช้ (สังเกตว่าคนละโดเมนกับ API ตัวอื่น)**

```bash
# ไฟล์ต้นฉบับ
curl -X GET https://api-data.line.me/v2/bot/message/{messageId}/content \
  -H 'Authorization: Bearer {channel access token}'

# ภาพย่อ (เล็กกว่ามาก เหมาะทำ thumbnail)
curl -X GET https://api-data.line.me/v2/bot/message/{messageId}/content/preview \
  -H 'Authorization: Bearer {channel access token}'

# เช็คว่า video/audio ไฟล์ใหญ่พร้อมให้ดึงหรือยัง
curl -X GET https://api-data.line.me/v2/bot/message/{messageId}/content/transcoding \
  -H 'Authorization: Bearer {channel access token}'
```

> ⚠️ **จุดที่พลาดกันบ่อยที่สุด:** โดเมนคือ **`api-data.line.me`** ไม่ใช่ `api.line.me` เพราะ LINE แยกโดเมนสำหรับรับส่งข้อมูลก้อนใหญ่ ถ้าใช้โดเมนผิดจะได้ 404 ทั้งที่ token ถูก

**ฝั่งโค้ดใช้ blob client ของ SDK** (ดูที่ `src/line/client.ts`)

```typescript
import { messagingApi } from '@line/bot-sdk'

// blob client คนละตัวกับ MessagingApiClient ปกติ เพราะชี้ไปคนละโดเมน
const blobClient = new messagingApi.MessagingApiBlobClient({
  channelAccessToken: config.channelAccessToken
})

// คืนค่าเป็น Readable stream ไม่ใช่ Buffer
// จึงเขียนลงไฟล์แบบ stream ได้เลย ไม่กินหน่วยความจำแม้ไฟล์ใหญ่
const stream = await blobClient.getMessageContent(messageId)
```

---

### 6.3 เก็บไว้ที่ไหนและตั้งชื่ออย่างไร

**สิ่งที่ห้ามทำ**

```
❌ เก็บด้วยชื่อไฟล์เดิมที่ผู้ใช้ส่งมา  ->  ชื่อซ้ำกันได้ และเสี่ยง path traversal
❌ เก็บ binary ลงคอลัมน์ในฐานข้อมูล   ->  ฐานข้อมูลบวมเร็วมาก backup ช้า
❌ วางไฟล์ในโฟลเดอร์ที่ Express serve เป็น static  ->  ใครเดา URL ถูกก็เปิดดูได้
```

**สิ่งที่ควรทำ** เก็บไฟล์นอกฐานข้อมูล แล้วให้ฐานข้อมูลเก็บแค่ **ตัวชี้** ซึ่งก็คือคอลัมน์ `storage_key` ในตาราง `media_files`

```
storage/
  2026/
    08/
      15/
        <lineMessageId>.jpg      <-- ใช้ lineMessageId เป็นชื่อ เพราะ unique อยู่แล้ว
        <lineMessageId>.pdf
```

| คอลัมน์ใน `media_files` | เก็บอะไร |
| --- | --- |
| `line_message_id` | UNIQUE กันดึงไฟล์เดิมซ้ำเมื่อ LINE retry (หลักการเดียวกับ `line_messages`) |
| `storage_key` | พาธสัมพัทธ์ เช่น `2026/08/15/5551....jpg` **ไม่เก็บ absolute path** เพื่อให้ย้ายที่เก็บได้ |
| `caption` | ชื่อไฟล์เดิมที่ผู้ใช้ส่ง (`message.fileName`) ไว้แสดงให้คนอ่าน ไม่ใช้เป็นชื่อไฟล์จริง |
| `status` | `pending` / `stored` / `failed` / `deleted` |

> 🎓 **จุดสอนเรื่อง `status`:** ทำไมต้องมี ไม่บันทึกเฉพาะที่สำเร็จไปเลย เพราะการดึงไฟล์เกิดหลังจากเราตอบ 200 ไปแล้ว ถ้าดึงพลาดกลางทางเราต้องรู้ว่า "เคยมีไฟล์นี้แต่ดึงไม่ได้" ไม่ใช่หายเงียบ และ view `v_media_gallery` จึงกรองเฉพาะ `status = 'stored'` เพื่อไม่ให้แถวที่ยังไม่พร้อมโผล่ในหน้าแกลเลอรี

**ขึ้น production ควรย้ายไป object storage**

| ที่เก็บ | เหมาะกับ | ข้อควรระวัง |
| --- | --- | --- |
| Local disk | คลาสนี้ / เซิร์ฟเวอร์ในองค์กรที่ควบคุมเอง | ต้องมีแผน backup และดูพื้นที่ให้พอ |
| S3 / Cloudflare R2 / Google Cloud Storage | ระบบจริงที่มีผู้ใช้หลายคน | `storage_key` ยังใช้รูปแบบเดิมได้ เปลี่ยนแค่โค้ดชั้นเขียนไฟล์ |
| ❌ ดิสก์ของ Render / Railway แบบ ephemeral | ไม่เหมาะ | ไฟล์หายทุกครั้งที่ deploy ใหม่ |

---

### 6.4 ลำดับการทำงานที่ถูกต้อง

```
LINE ส่ง event type=message, message.type=image เข้ามา
        │
        ▼
1. ตอบ 200 ทันที                          <-- กฎเหล็กข้อ 1 ยังใช้เหมือนเดิม
        │
        ▼
2. INSERT line_messages (metadata)         <-- ได้ข้อมูลไว้ก่อน แม้ไฟล์จะดึงไม่สำเร็จ
        │
        ▼
3. INSERT media_files status='pending'
        │
        ▼
4. เช็ค contentProvider.type === 'line'    <-- ถ้า external ข้ามไป
        │
        ▼
5. blobClient.getMessageContent(messageId)
   pipe ลงไฟล์ storage/YYYY/MM/DD/<id>.<ext>
        │
        ├── สำเร็จ  -> UPDATE status='stored', storage_key=...
        └── ล้มเหลว -> UPDATE status='failed' + log เหตุผล (ไม่ throw)
```

**Error ที่ต้องแยกให้ออก**

| สถานะ | ความหมาย | ควรทำอย่างไร |
| --- | --- | --- |
| `202 Accepted` | video/audio ไฟล์ใหญ่ LINE ยังเตรียมข้อมูลไม่เสร็จ | **ไม่ใช่ error** ให้เช็คด้วย endpoint `/content/transcoding` แล้วค่อยลองใหม่ |
| `404 Not Found` | messageId ไม่มีอยู่จริง | ตรวจว่าส่ง id ถูกตัวหรือไม่ |
| `410 Gone` | ผู้ใช้ **unsend** ข้อความนั้นไปแล้ว | ห้าม retry และควรลบ metadata ที่เก็บไว้ด้วย (ดูหัวข้อ 6.6) |

> ⚠️ **อย่าตั้ง retry แบบวนไม่จำกัด** ทั้ง 404 และ 410 คือ "ไม่มีทางสำเร็จ" การ retry ซ้ำมีแต่กินโควต้าและ rate limit ให้ retry เฉพาะกรณี 202 และ network error เท่านั้น และควรจำกัดจำนวนครั้ง

---

### 6.5 แสดงผลใน Admin Dashboard

view `v_media_gallery` join `media_files` กับ `line_groups` ให้แล้ว และกรองเฉพาะ `status = 'stored'` จึง query ตรงได้เลย

```sql
SELECT * FROM v_media_gallery
WHERE group_name = 'ทีมปฏิบัติการ'
  AND sent_at >= now() - interval '30 days'
ORDER BY sent_at DESC
```

**สอง endpoint ที่ต้องมี**

| เส้นทาง | ทำอะไร |
| --- | --- |
| `GET /admin/media` | หน้าแกลเลอรี แสดง thumbnail + ชื่อกลุ่ม + ผู้ส่ง + วันเวลา |
| `GET /admin/media/:id/file` | ส่งไฟล์จริงออกไป **หลังตรวจ session แล้วเท่านั้น** |

> ⚠️ **จุดออกแบบด้านความปลอดภัยที่สำคัญที่สุดของหัวข้อนี้:** ห้าม `app.use('/storage', express.static(...))` เพราะเท่ากับเปิดไฟล์ทั้งหมดให้ทุกคนที่รู้ URL ต้องให้ไฟล์ผ่าน route ที่ตรวจ session ก่อน แล้วจึงอ่านจากดิสก์ส่งออกไป
>
> และเมื่อประกอบพาธจาก `storage_key` ที่มาจากฐานข้อมูล ให้ตรวจด้วยเสมอว่าพาธผลลัพธ์ยังอยู่ใต้โฟลเดอร์ `storage/` จริง (ป้องกัน path traversal ในกรณีที่ข้อมูลถูกแก้)

> 💡 **ต่อยอดได้น่าสนใจ:** เมื่อมีไฟล์อยู่ในมือแล้ว สั่ง AI ผ่าน Postgres MCP ว่า *"เดือนนี้กลุ่มไหนส่งไฟล์เข้ามามากที่สุด และเป็นไฟล์ประเภทอะไรบ้าง"* ได้ทันที หรือถ้าต่อ OCR เข้าไปก็อ่านใบเสร็จที่ทีมส่งเข้ากลุ่มมาสรุปยอดได้

---

### 6.6 PDPA และ Governance (หัวข้อที่ห้ามข้าม)

การเก็บ **ข้อความ** จากกลุ่มมีความเสี่ยงระดับหนึ่ง แต่การเก็บ **ไฟล์** เสี่ยงกว่ามาก เพราะรูปที่คนส่งเข้ากลุ่มงานมักมีบัตรประชาชน สลิปโอนเงิน ใบเสร็จที่มีเลขบัญชี หรือเอกสารสัญญาปนอยู่โดยที่ผู้ส่งไม่ได้ตั้งใจให้ถูกเก็บถาวร

**หกข้อที่ต้องทำก่อนเปิดใช้จริงในองค์กร**

| # | สิ่งที่ต้องทำ | เหตุผล |
| --- | --- | --- |
| 1 | **แจ้งและขอความยินยอมให้ชัดว่า "เก็บไฟล์ด้วย"** | ข้อความ `join` ใน Workshop 3 บอกแค่ว่าบันทึกบทสนทนา ถ้าจะเก็บไฟล์ต้องเพิ่มประโยคนี้เข้าไป |
| 2 | **รองรับ unsend event ให้ครบ** | เอกสาร LINE ระบุตรง ๆ ว่าเมื่อผู้ใช้ unsend ให้ผู้ให้บริการเคารพเจตนา และ *"Delete the target message stored in a database or other storage device"* คือต้องลบทั้งแถวในฐานข้อมูล **และไฟล์บนดิสก์** |
| 3 | **ตั้ง retention policy** | เช่นลบไฟล์อายุเกิน 90 วันอัตโนมัติ ใช้ Task Scheduler ตัวเดียวกับ Workshop 5 ได้เลย เก็บเท่าที่จำเป็นต่อวัตถุประสงค์ |
| 4 | **จำกัดสิทธิ์เข้าถึง** | Dashboard ต้องล็อกอิน และควรแยกสิทธิ์ว่าใครดูไฟล์ของกลุ่มไหนได้ ไม่ใช่ล็อกอินแล้วเห็นทุกกลุ่ม |
| 5 | **เก็บ log ว่าใครเปิดดูไฟล์ไหนเมื่อไร** | ถ้าข้อมูลรั่ว ต้องตอบให้ได้ว่าเกิดจากทางไหน ตาราง `message_logs` มีแนวคิดนี้อยู่แล้ว ทำแบบเดียวกันกับฝั่งอ่านไฟล์ |
| 6 | **กำหนดผู้รับผิดชอบข้อมูล** | ต้องมีคนที่ตอบได้ว่าเก็บอะไร เก็บนานเท่าไร ลบอย่างไร และใครเข้าถึงได้บ้าง |

> ⚠️ **ข้อแนะนำที่ตรงไปตรงมาที่สุด:** ถ้ายังไม่มีเหตุผลทางธุรกิจที่ชัดเจนว่าต้องเก็บไฟล์ไว้ทำอะไร **อย่าเพิ่งเปิดใช้** เก็บเฉพาะ metadata แบบ Workshop 3 มีความเสี่ยงต่ำกว่ามาก และตอบโจทย์งานสรุปประชุมกับติดตามงานได้ครบอยู่แล้ว การเก็บข้อมูลที่ไม่ได้ใช้คือหนี้สิน ไม่ใช่สินทรัพย์

---

### ✅ Checkpoint Workshop 6

- [ ] อธิบายได้ว่าทำไม `api-data.line.me` ถึงคนละโดเมนกับ `api.line.me`
- [ ] บอกได้ว่าประเภทไหนดึงไฟล์จริงได้ ประเภทไหนได้แค่ metadata
- [ ] แยกความหมายของ `202` / `404` / `410` ได้ และรู้ว่าเคสไหนห้าม retry
- [ ] `SELECT * FROM v_media_gallery` แล้วเห็นแถวที่ `status = 'stored'`
- [ ] เปิดไฟล์ผ่าน Dashboard ได้ และยืนยันว่าเปิดตรง URL โดยไม่ล็อกอินไม่ได้
- [ ] อธิบายได้ว่าเมื่อได้ unsend event ต้องทำอะไรกับทั้งฐานข้อมูลและไฟล์

---

## 🧯 ปัญหาที่พบบ่อยในวันที่ 2 และวิธีแก้

### กลุ่ม 1: Webhook และ tunnel

| อาการ | สาเหตุ / วิธีแก้ |
| --- | --- |
| กด Verify ใน Console ไม่ผ่าน | 1) server ยังไม่รัน 2) URL ไม่ได้ต่อท้ายด้วย `/webhook` 3) tunnel ปิดไปแล้วหรือรันใหม่ (URL เปลี่ยนทุกครั้ง ต้องอัปเดตใน Console) |
| `signature validation failed` | `CHANNEL_SECRET` ไม่ตรง อย่าสับสนกับ access token secret อยู่ที่ tab Basic settings |
| Verify ผ่าน แต่พิมพ์ในกลุ่มไม่มี log | 1) ยังไม่เปิด "Use webhook" 2) บอทถูกเตะออกจากกลุ่ม 3) พิมพ์ในแชท 1:1 ไม่ใช่กลุ่ม (โค้ดกรองเฉพาะ `source.type === 'group'`) |
| ได้ log แต่ไม่เข้าฐานข้อมูล | ดู log ฝั่ง server มักเป็น `DATABASE_URL` ผิดหรือยังไม่ได้รัน `npm run db:setup` |
| ข้อความเข้าซ้ำในตาราง | LINE retry ได้เป็นปกติ ตรวจว่ามี `ON CONFLICT (line_message_id) DO NOTHING` แล้ว |
| LINE หยุดส่ง event มาให้เอง | webhook ตอบ error ซ้ำ ๆ ต้องแก้ให้ตอบ 200 เสมอ แล้วกด Verify ใหม่ |
| `ngrok` ไม่ใช่คำสั่งที่รู้จัก | ยังไม่ได้ใส่ใน PATH หรือแตกไฟล์ `ngrok.exe` ไว้คนละที่ ให้เปิด terminal ใหม่หลังติดตั้ง |
| ngrok ขึ้น `authentication failed` / `ERR_NGROK_4018` | ยังไม่ได้ผูก authtoken รัน `ngrok config add-authtoken <token>` แล้วสั่งใหม่ |
| ngrok ขึ้น `ERR_NGROK_108` (limited to 1 simultaneous session) | มี ngrok ค้างอยู่อีกหน้าต่าง ให้ปิด process เดิม หรือกด Stop ที่ Agents ในหน้า dashboard |
| เปิด URL แล้วเจอหน้าเตือนของ ngrok ก่อนเข้า | เป็นหน้า interstitial ของแพลนฟรี ไม่กระทบ Webhook เพราะ LINE ยิงแบบ POST ถ้าอยากปิดให้เพิ่ม header `ngrok-skip-browser-warning` ตอนทดสอบด้วยเบราว์เซอร์ |
| ngrok ต่อไม่ได้เลย (เครือข่ายบริษัทบล็อก) | ลองต่อผ่านเครือข่ายมือถือ (hotspot) ก่อน ถ้ายังไม่ได้ให้ใช้ตัวสำรอง `npx localtunnel --port 3000` หรือ `ssh -R 80:localhost:3000 nokey@localhost.run` |
| อยากรู้ว่า LINE ยิงอะไรมาบ้าง | เปิด http://127.0.0.1:4040 (ngrok Web Inspector) ดู request/response ย้อนหลังได้ทุกครั้ง |
| ข้อความเก่าก่อนบอทเข้ากลุ่มไม่เข้าระบบ | **ไม่ใช่บั๊ก** Messaging API ออกแบบมาอย่างนี้ |
| ดึงรูป/ไฟล์แล้วได้ 404 ทั้งที่ token ถูก | ใช้โดเมนผิด ต้องเป็น `api-data.line.me` ไม่ใช่ `api.line.me` (ดู Workshop 6) |
| ดึงไฟล์แล้วได้ `410 Gone` | ผู้ใช้ unsend ข้อความไปแล้ว ห้าม retry และควรลบ metadata ที่เก็บไว้ด้วย |
| ดึง video/audio แล้วได้ `202` | LINE ยังเตรียมไฟล์ไม่เสร็จ ไม่ใช่ error ให้เช็ค endpoint `/content/transcoding` ก่อนลองใหม่ |

### กลุ่ม 2: PostgreSQL

| อาการ | สาเหตุ / วิธีแก้ |
| --- | --- |
| `ECONNREFUSED 127.0.0.1:5432` | PostgreSQL ไม่ได้รัน Windows: เปิด services.msc หา postgresql แล้ว Start / Docker: `docker compose up -d` |
| `password authentication failed for user "postgres"` | รหัสผ่านใน `DATABASE_URL` ผิด แก้ในไฟล์ `.env` |
| `database "linechat" does not exist` | `psql -U postgres -c "CREATE DATABASE linechat;"` แล้วรัน `npm run db:setup` |
| `relation "line_messages" does not exist` | ยังไม่ได้สร้างตาราง รัน `npm run db:setup` |
| `psql` ไม่ใช่คำสั่งที่รู้จัก (Windows) | เพิ่ม `C:\Program Files\PostgreSQL\16\bin` เข้า PATH แล้วเปิด terminal ใหม่ |
| `could not determine data type of parameter $N` | พารามิเตอร์ใน `COALESCE`/`CASE` ต้องระบุชนิดให้ชัด เช่น `$4::boolean` |
| เวลาใน dashboard เพี้ยนไป 7 ชั่วโมง | คอลัมน์ต้องเป็น `TIMESTAMPTZ` และ query ต้องแปลง `AT TIME ZONE 'Asia/Bangkok'` |
| ต้องการล้างข้อมูลเริ่มใหม่ | `npm run db:reset` |

### กลุ่ม 3: Dashboard และ Task Scheduler

| อาการ | สาเหตุ / วิธีแก้ |
| --- | --- |
| `npm run dev` แล้ว `EADDRINUSE :3000` | มีอะไรใช้พอร์ต 3000 อยู่ Windows: `netstat -ano \| findstr :3000` แล้ว `taskkill /PID <pid> /F` หรือแก้ `PORT=3001` |
| ล็อกอิน Dashboard ไม่ได้ | ตรวจ `ADMIN_USERNAME` / `ADMIN_PASSWORD` ใน `.env` (ค่าเริ่มต้น admin / admin1234) |
| กราฟไม่ขึ้น หน้าขาว | Tailwind และ Chart.js โหลดจาก CDN ต้องมีอินเทอร์เน็ต |
| ส่งข้อความแล้วขึ้นสถานะ `mock` | ระบบอยู่ในโหมด MOCK ตั้ง `MOCK_LINE=false` และใส่ `CHANNEL_ACCESS_TOKEN` |
| กด "สรุปด้วย AI" ได้ผลลัพธ์แบบสถิติ ไม่ฉลาด | ยังไม่ได้ใส่ `ANTHROPIC_API_KEY` ระบบจึงใช้ตัวสรุป rule-based |
| ภาษาไทยใน CSV เปิดใน Excel เป็นตัวยึกยือ | เปิดด้วย Data > From Text/CSV แล้วเลือก UTF-8 |
| เซิร์ฟเวอร์ดับเองเมื่อเกิด error | ถ้าเขียน route เพิ่มเอง ต้องใช้ `asyncRouter()` ไม่ใช่ `express.Router()` เพราะ Express 4 ไม่จับ error จาก async handler |
| Task รันแล้วขึ้น `0x1` | เกือบทุกครั้งเพราะไม่ได้ตั้ง **Start in** ให้เป็นโฟลเดอร์โปรเจกต์ |
| `npm` ไม่ใช่คำสั่งที่รู้จักตอนรันผ่าน Task | Task Scheduler ไม่โหลด PATH ของผู้ใช้ ให้ใส่ path เต็มของ npm หรือใช้ `call npm` ในไฟล์ .bat |
| ตั้งเวลาไว้แต่ไม่รันตอน notebook ปิดฝา | ปลดติ๊ก "Start the task only if the computer is on AC power" |

---

## 📝 สรุปประจำวันที่ 2

| หัวข้อ | สิ่งที่ทำได้แล้ว |
| --- | --- |
| Module 3 - สถาปัตยกรรม | อธิบายสถาปัตยกรรม 4 ส่วนได้ และแยกงาน ad-hoc ออกจากงานประจำเป็น |
| ★ Workshop 3 - Group Chat Recorder | อ่านโค้ด Webhook Server เป็น เก็บแชทกลุ่มลง PostgreSQL ได้จริง เปิด tunnel และตั้ง Webhook URL เป็น |
| ★ Workshop 4 - AI สรุป + Dashboard | เชื่อม Postgres MCP วิเคราะห์บทสนทนาด้วยภาษาไทย ส่งสรุปกลับเข้ากลุ่มเป็น Flex และใช้ Dashboard เป็น |
| ★ Workshop 5 - Capstone | ใช้สคริปต์ daily-report ตรวจ anomaly 4 เกณฑ์ และตั้งเวลารันอัตโนมัติทุกเช้าได้ |
| Workshop 6 (เสริม) - เก็บไฟล์จากกลุ่ม | ดึงรูปและไฟล์เอกสารมาเก็บที่เซิร์ฟเวอร์เราเอง พร้อมข้อควรระวังด้าน PDPA |
| Module 4 - Governance | รู้แนวปฏิบัติด้าน Privacy สิทธิ์ฐานข้อมูล และสิ่งที่ต้องเพิ่มก่อนขึ้น production |

### 🔑 ห้าประโยคที่ต้องจำจากทั้งหลักสูตร

1. **เมื่อ API ปลายทางไม่ให้สิ่งที่เราต้องการ ให้เปลี่ยนจาก "ดึงเมื่อต้องใช้" เป็น "รับไว้ตั้งแต่ต้น"** นี่คือหลักคิดที่ใช้ได้กับทุกระบบ ไม่ใช่แค่ LINE
2. **หน่วยความจำกลางสำคัญกว่าบอทเก่ง ๆ ตัวเดียว** เพราะฐานข้อมูลเดียวรองรับได้ทั้ง 4 เส้นทาง (webhook, MCP, dashboard, สคริปต์)
3. **งาน ad-hoc ใช้ MCP สั่งสด งานประจำใช้สคริปต์ตั้งเวลา และ AI Agent คือคนเขียนสคริปต์ให้เราทั้งสองแบบ**
4. **แยกตรรกะออกจากทางเข้า** (`services/` และ `jobs/` ไม่รู้จัก express) ทำให้ย้ายจาก Task Scheduler ไป cloud ได้โดยไม่ต้องแก้โค้ด
5. **ทักษะที่แท้จริงคือการตรวจงาน AI** ไม่ใช่การเชื่อทุกอย่างที่ AI ตอบ ทั้งเรื่อง bounds ของ Rich Menu, SQL ที่ลืม timezone และเกณฑ์ anomaly ที่ false positive

### 🎯 ผลลัพธ์ที่ได้กลับไปใช้จริง

| สิ่งที่ได้ | ใช้ทำอะไรต่อ |
| --- | --- |
| **บอทแจ้งเตือน HR และประกาศภายใน** | ใช้กับงานสื่อสารภายในองค์กรได้ทันที |
| **ระบบเก็บแชทกลุ่ม + AI สรุป + ติดตามงาน** | ติดตั้งให้ทีมงานจริงได้หลังขอความยินยอม |
| **รายงานธุรกิจอัตโนมัติเข้ากลุ่มผู้บริหาร** | เปลี่ยน datasource เป็นฐานข้อมูลจริงขององค์กรได้เลย |
| **Prompt Library ภาษาไทย** | ปรับใช้กับงานจริงได้ทันที ใช้ได้ทั้ง Claude Code และ Codex |
| **Admin Dashboard 9 หน้า** | ต้นแบบระบบที่ส่งมอบให้ผู้ใช้ที่ไม่ใช่ Developer |

---

## 📖 เอกสารอ้างอิงของวันที่ 2

| หัวข้อ | ลิงก์ |
| --- | --- |
| LINE Messaging API - Receiving messages (Webhook) | https://developers.line.biz/en/docs/messaging-api/receiving-messages/ |
| LINE Messaging API - Group chats | https://developers.line.biz/en/docs/messaging-api/group-chats/ |
| LINE Messaging API - Get content (ดึงรูป/ไฟล์ที่ผู้ใช้ส่ง) | https://developers.line.biz/en/reference/messaging-api/#get-content |
| @line/bot-sdk (Node.js) | https://github.com/line/line-bot-sdk-nodejs |
| LINE Bot MCP Server (Official) | https://github.com/line/line-bot-mcp-server |
| LINE Flex Message Simulator | https://developers.line.biz/flex-simulator/ |
| Model Context Protocol | https://modelcontextprotocol.io/ |
| Claude Code - MCP | https://docs.claude.com/en/docs/claude-code/mcp |
| OpenAI Codex CLI | https://developers.openai.com/codex/cli/ |
| PostgreSQL 16 Documentation | https://www.postgresql.org/docs/16/ |
| ngrok Documentation | https://ngrok.com/docs/ |
| Schedule recurring tasks in Claude Cowork | https://support.claude.com/en/articles/13854387-schedule-recurring-tasks-in-claude-cowork |
| Schedule recurring tasks in Claude Code Desktop | https://code.claude.com/docs/en/desktop-scheduled-tasks |
| repo อ้างอิงของหลักสูตร | https://github.com/iamsamitdev/line-workflow-app |

---

**💡 คำคมประจำวัน:**

> "ระบบอัตโนมัติที่ดีที่สุดไม่ใช่ระบบที่ทำงานแทนเราได้ทุกอย่าง แต่คือระบบที่ทำงานซ้ำ ๆ แทนเราได้อย่างเสถียร แล้วคืนเวลาให้เราไปคิดในเรื่องที่เครื่องคิดแทนไม่ได้"

---

_เอกสารจัดทำโดย: อาจารย์สามิตร โกยม | สถาบันไอทีจีเนียส เอ็นจิเนียริ่ง_
_หลักสูตร LINE Workflow Automation with Claude Code & MCP (2026) - วันที่ 2 จาก 2_
_โทร. 02-570-8449 | มือถือ 088-807-9770 | www.itgenius.co.th_
